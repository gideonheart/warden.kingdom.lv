---
phase: 40-lifecycle-history-e2e-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/routes/instanceRoutes.ts
  - src/client/components/LifecycleEventsView.tsx
  - src/client/components/HistoryView.tsx
autonomous: true
requirements: [HIST-01, HIST-02]

must_haves:
  truths:
    - "History view has a 'Lifecycle' tab that displays session lifecycle events from the database"
    - "Lifecycle events table shows agent name/ID, timestamp, event type badge, outcome, and uptime"
    - "Events are filterable by agent ID (text input) and event type (dropdown: all/crashed/auto-restarted/idle-timeout/stopped)"
    - "Pagination works for large event sets (Next/Prev buttons, page indicator)"
    - "Force-kill endpoint logs a lifecycle event so history is complete"
    - "GET /api/lifecycle-events returns sensible defaults when limit/offset are non-numeric strings"
  artifacts:
    - path: "src/client/components/LifecycleEventsView.tsx"
      provides: "Lifecycle events table with filters and pagination"
      min_lines: 80
    - path: "src/client/components/HistoryView.tsx"
      provides: "Updated with Lifecycle tab entry"
    - path: "src/server/routes/instanceRoutes.ts"
      provides: "NaN-guarded lifecycle-events endpoint; force-kill lifecycle event logging"
  key_links:
    - from: "src/client/components/LifecycleEventsView.tsx"
      to: "/api/lifecycle-events"
      via: "fetch with ?agentId, ?eventType, ?limit, ?offset query params"
      pattern: "fetch.*api/lifecycle-events"
    - from: "src/client/components/HistoryView.tsx"
      to: "LifecycleEventsView"
      via: "import and tab render"
      pattern: "LifecycleEventsView"
---

<objective>
Add lifecycle events history section to the History view and close two tech-debt gaps that would leave the history incomplete.

Purpose: Complete HIST-01 and HIST-02 — operators can review all v3.4 lifecycle events (crashes, auto-restarts, idle-timeout stops, operator stops) with agent and event type filters. Without this UI, lifecycle events are written to the database but never exposed to the operator.

Output: New `LifecycleEventsView` component wired as the "Lifecycle" tab in `HistoryView`, plus NaN guard and force-kill event logging fixes in `instanceRoutes.ts`.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Prior plan context (relevant):
@.planning/phases/37-crash-detection-backend/37-02-SUMMARY.md

Key source files:
@src/shared/types.ts
@src/client/components/HistoryView.tsx
@src/client/components/SessionHistory.tsx
@src/server/routes/instanceRoutes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tech-debt gaps in instanceRoutes.ts (TD-4 and TD-5)</name>
  <files>src/server/routes/instanceRoutes.ts</files>
  <action>
Two targeted fixes to instanceRoutes.ts before building the UI that consumes these endpoints:

**TD-5 — NaN guard in GET /api/lifecycle-events (lines ~320-325):**

Replace the raw parseInt calls with NaN-safe versions. Current code:
```ts
const limit = typeof request.query.limit === 'string' ? parseInt(request.query.limit, 10) : undefined;
const offset = typeof request.query.offset === 'string' ? parseInt(request.query.offset, 10) : undefined;
```

Replace with:
```ts
const rawLimit = typeof request.query.limit === 'string' ? parseInt(request.query.limit, 10) : undefined;
const limit = rawLimit !== undefined && !Number.isNaN(rawLimit) ? rawLimit : undefined;
const rawOffset = typeof request.query.offset === 'string' ? parseInt(request.query.offset, 10) : undefined;
const offset = rawOffset !== undefined && !Number.isNaN(rawOffset) ? rawOffset : undefined;
```

This ensures a non-numeric limit/offset falls back to the `?? 50` / `?? 0` defaults in `DatabaseConnection.getLifecycleEvents()` rather than producing LIMIT 0 (empty results).

**TD-4 — Force-kill missing lifecycle event (POST /api/instances/:id/force-kill, lines ~296-316):**

Inside the try block, after `instanceTracker.updateStatus(instanceId, 'stopped')`, add lifecycle event logging. Model exactly on the operator-stop path in the regular `/stop` endpoint:

```ts
const uptimeSecs = (Date.now() - new Date(instance.createdAt).getTime()) / 1000;
const projectSlug = path.basename(instance.projectPath) || instance.agentId;
database.insertLifecycleEvent({
  sessionId: instance.id,
  agentId: instance.agentId,
  sessionName: instance.tmuxSessionName,
  eventType: 'stopped',
  outcome: 'force-killed',
  uptimeSecs,
  projectSlug,
  lastKnownState: instance.status,
  stopReason: 'operator-stop',
});
```

Place this BEFORE the `response.json(...)` call. Wrap in the existing try/catch so a logging failure doesn't break the stop response.

Verify `database` is already imported in scope (it is, confirmed from file read). `path` is also already imported.

Run `npx tsc --noEmit` after the edit to confirm no type regressions.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm force-kill route now calls database.insertLifecycleEvent and NaN guard is in place for limit/offset</manual>
    <sampling_rate>run after this task commits, before Task 2 begins</sampling_rate>
  </verify>
  <done>
    - GET /api/lifecycle-events: passing ?limit=abc returns default 50 events, not 0
    - POST /api/instances/:id/force-kill: a session_lifecycle_events row is inserted on every force-kill
    - TypeScript compiles with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LifecycleEventsView component and add Lifecycle tab to HistoryView</name>
  <files>src/client/components/LifecycleEventsView.tsx
src/client/components/HistoryView.tsx</files>
  <action>
**Create `src/client/components/LifecycleEventsView.tsx`:**

New component following the SessionHistory.tsx pattern (fetch on mount and on filter change, pagination via page state, warden-* theme tokens, same filter UX structure).

State:
- `events: LifecycleEvent[]` (from `@shared/types.ts`)
- `total: number`
- `page: number` (0-indexed)
- `isLoading: boolean`
- `filterAgentId: string` (text input)
- `filterEventType: string` (dropdown: `''` = all, `'crashed'`, `'auto-restarted'`, `'idle-timeout'`, `'stopped'`)

Page size: 25 events per page.

Fetch on mount and when filters/page change (useEffect deps: [filterAgentId, filterEventType, page]). Fetch `/api/lifecycle-events` with params: `agentId`, `eventType`, `limit=25`, `offset=page*25`. Use `useCallback` for `fetchEvents`.

Event type badge colors (use Tailwind bg-* classes, keep warden panel style):
- `crashed` → `bg-red-900/40 text-red-300 border border-red-700`
- `auto-restarted` → `bg-blue-900/40 text-blue-300 border border-blue-700`
- `idle-timeout` → `bg-yellow-900/40 text-yellow-300 border border-yellow-700`
- `stopped` → `bg-zinc-700/40 text-zinc-400 border border-zinc-600`
- `started` → `bg-green-900/40 text-green-300 border border-green-700`
- fallback → `bg-zinc-700/40 text-zinc-300 border border-zinc-600`

Table columns: Timestamp | Agent | Session | Event Type | Outcome | Uptime

Timestamp: format ISO string as local datetime using `new Date(event.timestamp).toLocaleString()`.
Uptime: show as `{Math.round(event.uptimeSecs ?? 0)}s` or `—` if null.
Outcome: display raw string or `—` if null.

Filters row: text input (`placeholder="Filter by agent ID"`, `value={filterAgentId}`, reset page to 0 on change) + event type `<select>` with All/Crashed/Auto-restarted/Idle-timeout/Stopped/Started options.

Pagination: show `Page {page+1} of {Math.ceil(total/25) || 1}` and Prev/Next buttons. Disable Prev on page 0, disable Next when `(page+1)*25 >= total`.

Empty state: "No lifecycle events found." centered in the table area.

Loading state: show spinner (reuse the `.animate-spin` pattern visible in other components) while `isLoading` is true.

Import `LifecycleEvent` from `'@shared/types.js'`.

Use `warden-panel`, `warden-border`, `warden-text`, `warden-text-dim`, `warden-accent` tokens throughout. Match SessionHistory.tsx styling conventions exactly (table with `border-collapse`, `border-warden-border`, `text-sm`, row hover states).

**Update `src/client/components/HistoryView.tsx`:**

1. Add `'lifecycle'` to the `HistoryTab` type: `type HistoryTab = 'sessions' | 'tokens' | 'logs' | 'notifications' | 'lifecycle';`

2. Add tab entry to the `tabs` array: `{ id: 'lifecycle', label: 'Lifecycle' }` — place it after `'sessions'` (second position) so it is prominent.

3. Import `LifecycleEventsView` from `'./LifecycleEventsView.js'`.

4. In the desktop tab content: add `{activeTab === 'lifecycle' && <LifecycleEventsView />}` alongside existing tab renders.

5. In the mobile accordion: add `<MobileAccordionSection title="Lifecycle"><LifecycleEventsView /></MobileAccordionSection>` after the Sessions accordion.

6. Update the existing `dashboard.spec.ts` E2E test assertion that checks the History view tabs — it currently asserts `getByRole('button', { name: 'Activity' })` and `getByRole('button', { name: 'Sessions' })`. Add a check for `getByRole('button', { name: 'Lifecycle' })` in the test that switches to history view. (Note: this test file will be expanded more in plan 40-02, but the new tab must be discoverable by Playwright now.)

Run `npm run build` after changes to confirm production build succeeds.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build 2>&1 | tail -20</automated>
    <manual>Check that HistoryView has a Lifecycle tab button and LifecycleEventsView renders a table with filter controls</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    - `src/client/components/LifecycleEventsView.tsx` exists (min 80 lines) with filter controls, table, pagination
    - History view shows a "Lifecycle" tab button (desktop) and accordion section (mobile)
    - Clicking the Lifecycle tab renders LifecycleEventsView with agentId text filter + eventType dropdown
    - Production build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` succeeds (Vite client + tsc server)
- GET /api/lifecycle-events with ?limit=abc returns 50 events (default, not 0)
- POST /api/instances/:id/force-kill inserts a session_lifecycle_events row
- HistoryView has 5 tabs including "Lifecycle"
- LifecycleEventsView renders filter controls (agentId input + eventType select) and a table
- Pagination controls appear (even if only one page of data)
- Empty state renders gracefully when no events in DB
</verification>

<success_criteria>
HIST-01: History view Lifecycle tab shows session_lifecycle_events (crashes, auto-restarts, idle-timeout stops, operator stops) with agent, timestamp, event type badge, and outcome.
HIST-02: Events are filterable by agent ID (text input) and event type (dropdown with all v3.4 event types).
Tech debt TD-4 and TD-5 are both closed.
TypeScript compiles and production build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/40-lifecycle-history-e2e-verification/40-01-SUMMARY.md`
</output>
