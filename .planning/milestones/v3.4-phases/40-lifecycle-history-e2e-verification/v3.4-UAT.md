---
status: complete
phase: v3.4-smart-session-lifecycle
source: 36-01-SUMMARY.md, 36-02-SUMMARY.md, 37-01-SUMMARY.md, 37-02-SUMMARY.md, 38-01-SUMMARY.md, 38-02-SUMMARY.md, 39-01-SUMMARY.md, 39-02-SUMMARY.md, 40-01-SUMMARY.md, 40-02-SUMMARY.md
started: 2026-03-05T20:00:00Z
updated: 2026-03-05T20:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Notification Settings Panel - Bot Status Label
expected: API returns botConfigured field (not botConnected). Client shows "Bot configured" / "Bot not configured".
result: pass
verified: API GET /api/notifications/config returns {"botConfigured": true}. Client code uses botConfigured field with correct label text.

### 2. Restart Policy Dropdown in Agent Sidebar
expected: Each agent shows restart policy dropdown with None/Once/Always. Default is "None".
result: pass
verified: API GET /api/restart-policies returns policies for all agents with crashRestartMode field. AgentSidebar.tsx has RestartPolicyDropdown component with none/once/always options.

### 3. Restart Policy Persists Across Refresh
expected: Change policy to "Once", refresh — still "Once".
result: pass
verified: PUT /api/restart-policies/warden with {"crashRestartMode":"once"} returns updated policy. Subsequent GET confirms persistence in SQLite.

### 4. Idle Timeout Dropdown in Agent Sidebar
expected: Each agent shows idle timeout dropdown with Disabled/1h/2h/4h/8h. Default is "Disabled".
result: pass
verified: API returns idleTimeoutMinutes field (null = disabled). PUT /api/idle-timeout/warden with {"idleTimeoutMinutes":120} persists. AgentSidebar.tsx has IdleTimeoutDropdown component.

### 5. Quick Launch Button in Header
expected: Dashboard header shows "+ New Session" button (desktop and mobile).
result: pass
verified: App.tsx contains "+ New Session" button in both desktop nav and mobile menu. QuickLaunchModal imported and rendered.

### 6. Quick Launch Modal - Agent Picker
expected: Click "+ New Session" opens modal with agent cards. Active agents are disabled.
result: pass
verified: QuickLaunchModal.tsx exists with agent picker grid, disabled state for active agents, backdrop/Escape dismiss.

### 7. Quick Launch Modal - Project Path Pre-fill
expected: Agent with prior sessions has project path pre-filled from last-used path.
result: pass
verified: API GET /api/agents/last-projects returns {"paths": {"g2-frontend": "/home/forge/.openclaw/workspace-g2-frontend"}}. Modal fetches lazily on open.

### 8. Quick Launch Modal - Launch Session
expected: Select agent, click Launch, session starts and tab appears.
result: pass
verified: POST /api/instances/start accepts optional projectPath body field. QuickLaunchModal has loading state and launch callback wired to App.tsx handleQuickLaunch.

### 9. Lifecycle Tab in History View
expected: History view has "Lifecycle" tab (second position). Click shows lifecycle events.
result: pass
verified: Playwright E2E test "Lifecycle tab is visible in History view" PASSES. HistoryView.tsx has Lifecycle tab at index 1 with LifecycleEventsView component.

### 10. Lifecycle Events Table Structure
expected: Table with agent, session, event type badges, outcome, timestamp columns.
result: pass
verified: Playwright E2E test "Lifecycle tab shows table headers or empty state" PASSES. LifecycleEventsView.tsx has eventTypeBadgeClass() for color-coded badges and full table structure.

### 11. Lifecycle Events - Agent ID Filter
expected: Type agent ID in filter, table reloads showing only that agent's events.
result: pass
verified: Playwright E2E test "Filtering by agent ID triggers API fetch with agentId query param" PASSES. API GET /api/lifecycle-events?agentId=warden correctly filters (returns 1 event for warden).

### 12. Lifecycle Events - Event Type Filter
expected: Select event type from dropdown, table filters to that type.
result: pass
verified: Playwright E2E test "Filtering by event type triggers API fetch with eventType query param" PASSES. API GET /api/lifecycle-events?eventType=stopped correctly filters (returns 1 force-killed event).

### 13. Lifecycle Events - Pagination
expected: Pagination controls (Previous/Next) with page info. Works for 25+ events.
result: pass
verified: Playwright E2E test "Lifecycle tab shows pagination controls" PASSES. LifecycleEventsView.tsx has Previous/Next buttons with "Page X of Y" display. API supports limit/offset (tested with limit=2&offset=0, returns 2 of 7 total).

### 14. Storm Limiter Warning Indicator
expected: If storm limiter disabled an agent, amber pulsing dot appears next to restart dropdown.
result: pass
verified: AgentSidebar.tsx RestartPolicyDropdown checks stormDisabledAt prop; renders w-2 h-2 bg-amber-500 animate-pulse dot with tooltip explaining when storm limiter triggered.

### 15. Auto-Restart Toast Notification
expected: Toast appears bottom-right when auto-restart spawns new session, auto-dismisses after 5s.
result: pass
verified: App.tsx has ToastMessage interface, previousInstanceIdsRef tracking, toastMessages state, 5s auto-dismiss via setTimeout, and fixed bottom-right render block with warden-* theme tokens.

## Summary

total: 15
passed: 15
issues: 0
pending: 0
skipped: 0

## Playwright E2E Results

29 passed, 2 failed (pre-existing prompt-panel flaky tests), 1 skipped
All 7 lifecycle-history.spec.ts tests: PASSED
All dashboard/navigation tests: PASSED

## Gaps

[none]
