---
phase: 15-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/gsdTypes.ts
  - src/server/services/GsdRegistryService.ts
  - src/server/routes/gsdRoutes.ts
  - src/client/hooks/useGsdRegistry.ts
  - src/client/hooks/useAgentLiveStatus.ts
  - src/client/components/GsdView.tsx
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "src/shared/gsdTypes.ts exists and exports RegistryAgent, GsdRegistry, AgentStateHint, and PressureLevel"
    - "GsdRegistryService.ts imports RegistryAgent and GsdRegistry from @shared/gsdTypes.ts and has no local interface declarations for those types"
    - "gsdRoutes.ts imports AgentStateHint and PressureLevel from @shared/gsdTypes.ts and has no local type declarations for those types"
    - "useGsdRegistry.ts imports RegistryAgent and GsdRegistry from @shared/gsdTypes.ts and has no local interface declarations for those types"
    - "useAgentLiveStatus.ts imports AgentStateHint and PressureLevel from @shared/gsdTypes.ts and has no local type declarations for those types"
    - "GsdView.tsx imports AgentStateHint and PressureLevel from @shared/gsdTypes.ts (not from useAgentLiveStatus)"
    - "npm run typecheck passes with zero errors"
    - "npm run build succeeds"
  artifacts:
    - path: "src/shared/gsdTypes.ts"
      provides: "Single source of truth for GSD types shared across client and server"
      exports: ["RegistryAgent", "GsdRegistry", "AgentStateHint", "PressureLevel"]
      min_lines: 15
    - path: "src/server/services/GsdRegistryService.ts"
      provides: "GSD registry service with shared type imports"
    - path: "src/server/routes/gsdRoutes.ts"
      provides: "GSD API routes with shared type imports"
    - path: "src/client/hooks/useGsdRegistry.ts"
      provides: "GSD registry React hook with shared type imports"
    - path: "src/client/hooks/useAgentLiveStatus.ts"
      provides: "Agent live status React hook with shared type imports"
  key_links:
    - from: "src/server/services/GsdRegistryService.ts"
      to: "src/shared/gsdTypes.ts"
      via: "import type"
      pattern: "import.*type.*from.*@shared/gsdTypes"
    - from: "src/server/routes/gsdRoutes.ts"
      to: "src/shared/gsdTypes.ts"
      via: "import type"
      pattern: "import.*type.*from.*@shared/gsdTypes"
    - from: "src/client/hooks/useGsdRegistry.ts"
      to: "src/shared/gsdTypes.ts"
      via: "import type and re-export"
      pattern: "import.*type.*from.*@shared/gsdTypes"
    - from: "src/client/hooks/useAgentLiveStatus.ts"
      to: "src/shared/gsdTypes.ts"
      via: "import type and re-export"
      pattern: "import.*type.*from.*@shared/gsdTypes"
    - from: "src/client/components/GsdView.tsx"
      to: "src/shared/gsdTypes.ts"
      via: "import type"
      pattern: "import.*type.*from.*@shared/gsdTypes"
---

<objective>
Create a single shared GSD types module at `src/shared/gsdTypes.ts` and update all server and client files to import from it, eliminating duplicate type declarations across 4 files.

Purpose: Establishes a single source of truth for GSD types (`RegistryAgent`, `GsdRegistry`, `AgentStateHint`, `PressureLevel`) so that future changes to these types only need to be made in one place. This is the dependency anchor for all Phase 16 DRY/SRP work.
Output: One new shared types file, four files with updated imports, zero duplicate type definitions.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/shared/types.ts
@src/server/services/GsdRegistryService.ts
@src/server/routes/gsdRoutes.ts
@src/client/hooks/useGsdRegistry.ts
@src/client/hooks/useAgentLiveStatus.ts
@src/client/components/GsdView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/shared/gsdTypes.ts with unified GSD types</name>
  <files>src/shared/gsdTypes.ts</files>
  <action>
Create `src/shared/gsdTypes.ts` with these four exported type definitions (canonical source, merge from GsdRegistryService.ts and useAgentLiveStatus.ts):

```typescript
// GSD shared types — single source of truth for client and server

export interface RegistryAgent {
  agent_id: string;
  enabled: boolean;
  working_directory: string;
  tmux_session_name: string;
  claude_launch_command: string;
  auto_wake: boolean;
  topic_id: number;
  openclaw_session_id: string;
  claude_resume_target: string;
  claude_post_launch_mode: string;
}

export interface GsdRegistry {
  global_status_openclaw_session_id: string;
  global_status_openclaw_session_key: string;
  agents: RegistryAgent[];
}

export type AgentStateHint = 'working' | 'idle' | 'menu' | 'permission_prompt' | 'error';

export type PressureLevel = 'ok' | 'warning' | 'critical';
```

The `RegistryAgent` and `GsdRegistry` interfaces are identical in both `GsdRegistryService.ts` (server) and `useGsdRegistry.ts` (client). The `AgentStateHint` and `PressureLevel` types are identical in `gsdRoutes.ts` (server) and `useAgentLiveStatus.ts` (client). Place the canonical definitions here.

Follow the existing `@shared` path alias convention (already configured in tsconfig.json and vite.config.ts).
  </action>
  <verify>
Run `npm run typecheck` -- must pass (new file only, no consumers yet changed).
Confirm file exists: `ls src/shared/gsdTypes.ts`
  </verify>
  <done>src/shared/gsdTypes.ts exists and exports RegistryAgent, GsdRegistry, AgentStateHint, PressureLevel</done>
</task>

<task type="auto">
  <name>Task 2: Update server and client imports to use shared GSD types</name>
  <files>
    src/server/services/GsdRegistryService.ts
    src/server/routes/gsdRoutes.ts
    src/client/hooks/useGsdRegistry.ts
    src/client/hooks/useAgentLiveStatus.ts
    src/client/components/GsdView.tsx
  </files>
  <action>
Update five files to import types from `@shared/gsdTypes.ts` instead of declaring them locally:

**1. `src/server/services/GsdRegistryService.ts`:**
- Add: `import type { RegistryAgent, GsdRegistry } from '@shared/gsdTypes.js';`
- Delete: The local `export interface RegistryAgent { ... }` block (lines 6-17)
- Delete: The local `export interface GsdRegistry { ... }` block (lines 19-23)
- Keep: The `export` on class methods/singleton -- the service still needs to be exported
- Add re-exports so existing importers of these types from this file continue to work during transition: `export type { RegistryAgent, GsdRegistry } from '@shared/gsdTypes.js';`

**2. `src/server/routes/gsdRoutes.ts`:**
- Add: `import type { AgentStateHint, PressureLevel } from '@shared/gsdTypes.js';`
- Delete: The local `type AgentStateHint = ...` declaration (line 28)
- Delete: The local `type PressureLevel = ...` declaration (line 29)
- Keep: All helper functions (`detectAgentState`, `extractContextPressure`) and route handlers unchanged

**3. `src/client/hooks/useGsdRegistry.ts`:**
- Add: `import type { RegistryAgent, GsdRegistry } from '@shared/gsdTypes.js';`
- Delete: The local `export interface RegistryAgent { ... }` block (lines 5-16)
- Delete: The local `export interface GsdRegistry { ... }` block (lines 18-22)
- Add re-exports so GsdView.tsx (which currently uses the hook return type implicitly) continues to work: `export type { RegistryAgent, GsdRegistry } from '@shared/gsdTypes.js';`

**4. `src/client/hooks/useAgentLiveStatus.ts`:**
- Add: `import type { AgentStateHint, PressureLevel } from '@shared/gsdTypes.js';`
- Delete: The local `export type AgentStateHint = ...` declaration (line 5)
- Delete: The local `export type PressureLevel = ...` declaration (line 6)
- Add re-exports so existing consumers continue to work: `export type { AgentStateHint, PressureLevel } from '@shared/gsdTypes.js';`

**5. `src/client/components/GsdView.tsx`:**
- Change: `import type { AgentStateHint, PressureLevel } from '../hooks/useAgentLiveStatus.js';` to `import type { AgentStateHint, PressureLevel } from '@shared/gsdTypes.js';`
- This is a direct import from the shared module rather than going through the hook's re-export

IMPORTANT: The re-exports (`export type { X } from '@shared/gsdTypes.js'`) in GsdRegistryService.ts, useGsdRegistry.ts, and useAgentLiveStatus.ts are necessary so that any other files importing these types from the old locations continue to compile. This is a safe migration pattern. Phase 16 may later clean up these re-exports when it refactors consumers.
  </action>
  <verify>
Run `npm run typecheck` -- must pass with zero errors.
Run `npm run build` -- must succeed.
Grep for local type/interface declarations that should no longer exist:
- `grep -n "^export interface RegistryAgent" src/server/services/GsdRegistryService.ts` -- must return nothing
- `grep -n "^export interface GsdRegistry" src/server/services/GsdRegistryService.ts` -- must return nothing
- `grep -n "^export interface RegistryAgent" src/client/hooks/useGsdRegistry.ts` -- must return nothing
- `grep -n "^export interface GsdRegistry" src/client/hooks/useGsdRegistry.ts` -- must return nothing
- `grep -n "^export type AgentStateHint" src/client/hooks/useAgentLiveStatus.ts` -- must return nothing (only re-export allowed)
- `grep -n "^export type PressureLevel" src/client/hooks/useAgentLiveStatus.ts` -- must return nothing (only re-export allowed)
- `grep -n "^type AgentStateHint" src/server/routes/gsdRoutes.ts` -- must return nothing
- `grep -n "^type PressureLevel" src/server/routes/gsdRoutes.ts` -- must return nothing
Confirm GsdView.tsx imports from shared: `grep "gsdTypes" src/client/components/GsdView.tsx` -- must match
  </verify>
  <done>All 5 files import GSD types from @shared/gsdTypes.ts, no local type declarations remain, typecheck and build pass</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm run build` completes successfully
3. `src/shared/gsdTypes.ts` exists and exports exactly 4 types: RegistryAgent, GsdRegistry, AgentStateHint, PressureLevel
4. No local type declarations for these 4 types exist in any file other than gsdTypes.ts (re-exports are acceptable)
5. Server files import from `@shared/gsdTypes.js`
6. Client files import from `@shared/gsdTypes.js`
</verification>

<success_criteria>
- Single source of truth for GSD types established at src/shared/gsdTypes.ts
- All 4 duplicate type declaration sites replaced with imports from shared module
- Re-exports preserve backward compatibility for any transitive importers
- Zero type errors, zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/15-02-SUMMARY.md`
</output>
