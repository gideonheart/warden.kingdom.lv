---
phase: 15-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/plugins/gsd-manager-plugin.tsx
  - src/client/components/AgentsView.tsx
autonomous: true
requirements:
  - DEAD-01
  - DEAD-02

must_haves:
  truths:
    - "gsd-manager-plugin.tsx contains only DisabledPanel and plugin manifest/export -- no dead GsdManagerPanelExpanded body"
    - "AgentsView.tsx does not exist in the codebase"
    - "npm run typecheck passes with zero errors after dead code removal"
    - "npm run build succeeds after dead code removal"
  artifacts:
    - path: "src/client/plugins/gsd-manager-plugin.tsx"
      provides: "Disabled bottom-panel plugin stub"
      contains: "DisabledPanel"
      max_lines: 25
  key_links:
    - from: "src/client/plugins/gsd-manager-plugin.tsx"
      to: "@shared/pluginTypes.js"
      via: "PluginManifest and PluginModule imports"
      pattern: "import.*pluginTypes"
---

<objective>
Delete ~750 lines of dead code from two files: gut the dead GsdManagerPanelExpanded component body from gsd-manager-plugin.tsx (keeping only the DisabledPanel stub and manifest), and delete the orphaned AgentsView.tsx entirely.

Purpose: Removes noise before Phase 16 extraction work, shrinks diff surface area, and eliminates misleading dead code that duplicates live code in GsdView.tsx.
Output: A leaner codebase with only the active GsdView.tsx remaining as the GSD UI entry point.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/client/plugins/gsd-manager-plugin.tsx
@src/client/components/AgentsView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip dead GsdManagerPanelExpanded from gsd-manager-plugin.tsx</name>
  <files>src/client/plugins/gsd-manager-plugin.tsx</files>
  <action>
Rewrite `src/client/plugins/gsd-manager-plugin.tsx` to contain ONLY:
1. The `PluginManifest` and `PluginModule` type imports from `@shared/pluginTypes.js`
2. The `manifest` const (lines 10-17 of original)
3. The `DisabledPanel` function that returns `null` (lines 534-536 of original)
4. The default export `{ manifest, PanelComponent: DisabledPanel } satisfies PluginModule`
5. A brief comment explaining GSD content moved to GsdView.tsx nav page

Remove ALL other code: the `useState/useCallback/useMemo` React imports, all hook imports (`useGsdRegistry`, `useGsdHookFeed`, `useActiveInstances`, `useAgentLiveStatus`, `useAgentStateFiles`), the `AgentStateHint`/`PressureLevel` type imports, all helper components (`CopyButton`, `BashHint`, `StateBadge`, `PressureIndicator`, `PhaseProgress`), all color constant maps (`STATUS_COLORS`, `STATE_BADGE_COLORS`, `STATE_LABELS`, `PRESSURE_COLORS`), tab definitions (`TABS`, `TabId`), and the entire `GsdManagerPanelExpanded` function body.

The resulting file should be approximately 20 lines.
  </action>
  <verify>
Run `npm run typecheck` -- must pass with zero errors.
Run `wc -l src/client/plugins/gsd-manager-plugin.tsx` -- should be under 25 lines.
Grep for `GsdManagerPanelExpanded` in `src/client/plugins/gsd-manager-plugin.tsx` -- must return zero matches.
  </verify>
  <done>gsd-manager-plugin.tsx exports only DisabledPanel, is under 25 lines, and typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned AgentsView.tsx</name>
  <files>src/client/components/AgentsView.tsx</files>
  <action>
Delete `src/client/components/AgentsView.tsx` entirely using `rm`.

Before deleting, confirm no file in the codebase imports AgentsView (grep for `AgentsView` across `src/` -- it should only appear in AgentsView.tsx itself, not in any import statements elsewhere). If any import references are found, remove them before deleting the file.

This file is an orphan: it was superseded by the Agents tab inside `GsdView.tsx` (created during Quick-6) and is not imported anywhere.
  </action>
  <verify>
Run `ls src/client/components/AgentsView.tsx` -- must fail (file not found).
Grep for `AgentsView` across `src/` -- must return zero matches.
Run `npm run typecheck` -- must pass with zero errors.
  </verify>
  <done>AgentsView.tsx does not exist, no dangling imports, typecheck passes</done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm run build` completes successfully
3. `wc -l src/client/plugins/gsd-manager-plugin.tsx` shows under 25 lines
4. `grep -r "GsdManagerPanelExpanded" src/` returns no results
5. `grep -r "AgentsView" src/` returns no results
6. `ls src/client/components/AgentsView.tsx` fails (file deleted)
</verification>

<success_criteria>
- ~750 lines of dead code removed (538 from plugin gutting + 220 from AgentsView deletion)
- Zero import errors or type errors
- Build succeeds
- Only DisabledPanel remains in gsd-manager-plugin.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/15-01-SUMMARY.md`
</output>
