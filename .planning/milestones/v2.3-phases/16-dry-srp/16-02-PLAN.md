---
phase: 16-dry-srp
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - src/client/components/AgentsTab.tsx
  - src/client/components/ControlsTab.tsx
  - src/client/components/RegistryTab.tsx
  - src/client/components/HooksTab.tsx
  - src/client/components/GsdView.tsx
autonomous: true
requirements:
  - SRP-01
  - SRP-02
  - SRP-03
  - SRP-04
  - SRP-05

must_haves:
  truths:
    - "AgentsTab.tsx renders the agent card grid with state badges, pressure indicators, phase progress, and enabled toggles"
    - "ControlsTab.tsx renders spawn form and command dispatch form with SearchableSelect dropdowns and BashHint"
    - "RegistryTab.tsx renders the registry table with enabled toggles"
    - "HooksTab.tsx renders the hook event feed table"
    - "GsdView.tsx is under 100 lines and contains only tab state management, data hooks, and tab routing"
    - "All GSD UI behaviors work identically after decomposition — no visual regressions"
  artifacts:
    - path: "src/client/components/AgentsTab.tsx"
      provides: "Agents tab content extracted from GsdView.tsx"
      exports: ["AgentsTab"]
    - path: "src/client/components/ControlsTab.tsx"
      provides: "Controls tab content extracted from GsdView.tsx"
      exports: ["ControlsTab"]
    - path: "src/client/components/RegistryTab.tsx"
      provides: "Registry tab content extracted from GsdView.tsx"
      exports: ["RegistryTab"]
    - path: "src/client/components/HooksTab.tsx"
      provides: "Hooks tab content extracted from GsdView.tsx"
      exports: ["HooksTab"]
    - path: "src/client/components/GsdView.tsx"
      provides: "Thin tab router shell under 100 LOC"
      exports: ["GsdView"]
  key_links:
    - from: "src/client/components/GsdView.tsx"
      to: "src/client/components/AgentsTab.tsx"
      via: "named import and conditional render"
      pattern: "import.*AgentsTab.*from.*AgentsTab"
    - from: "src/client/components/GsdView.tsx"
      to: "src/client/components/ControlsTab.tsx"
      via: "named import and conditional render"
      pattern: "import.*ControlsTab.*from.*ControlsTab"
    - from: "src/client/components/AgentsTab.tsx"
      to: "src/client/components/gsdShared.tsx"
      via: "imports STATUS_COLORS, StateBadge, PressureIndicator, PhaseProgress"
      pattern: "import.*from.*gsdShared"
    - from: "src/client/components/ControlsTab.tsx"
      to: "src/client/components/gsdShared.tsx"
      via: "imports BashHint"
      pattern: "import.*BashHint.*from.*gsdShared"
---

<objective>
Extract each of the 4 GsdView tabs into standalone component files and reduce GsdView.tsx to a thin tab router under 100 lines.

Purpose: Each tab becomes independently maintainable, testable, and (in Phase 17) lazy-mountable. GsdView.tsx becomes a pure routing shell with no business logic.

Output: 4 new tab component files + slimmed GsdView.tsx under 100 LOC.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dry-srp/16-01-SUMMARY.md
@src/client/components/GsdView.tsx
@src/client/components/gsdShared.tsx
@src/client/hooks/useGsdRegistry.ts
@src/client/hooks/useGsdHookFeed.ts
@src/client/hooks/useAgentLiveStatus.ts
@src/client/hooks/useAgentStateFiles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AgentsTab and ControlsTab components</name>
  <files>src/client/components/AgentsTab.tsx, src/client/components/ControlsTab.tsx</files>
  <action>
**AgentsTab.tsx:**

Create `src/client/components/AgentsTab.tsx` exporting `AgentsTab` component.

Props interface `AgentsTabProps`:
- `agents: RegistryAgent[]` — agent list from registry
- `instances: AgentInstance[]` — all instances for status lookup
- `liveStatus: Map<string, { state: AgentStateHint | null; contextPressure: number | null; contextPressureLevel: PressureLevel | null }>` — live agent status
- `stateFiles: Map<string, { phase: string | null; progress: number | null }>` — parsed STATE.md data
- `getEffectiveEnabled: (agentId: string, serverEnabled: boolean) => boolean` — from useGsdRegistry
- `toggleEnabled: (agentId: string, currentEnabled: boolean) => void` — from useGsdRegistry
- `registryLoading: boolean`
- `registryError: string | null`

Imports from `gsdShared.tsx`: `STATUS_COLORS`, `StateBadge`, `PressureIndicator`, `PhaseProgress`.
Imports from `@shared/gsdTypes.js`: `RegistryAgent`, `AgentStateHint`, `PressureLevel`.
Imports from `@shared/types.js`: `AgentInstance` (for instances prop typing).

Body: Move the entire `{activeTab === 'agents' && (...)}` JSX block from GsdView.tsx (the agent card grid with "No agents registered" empty state, the grid of cards with status dot, StateBadge, PressureIndicator, PhaseProgress, and enabled toggle). Remove the `activeTab` conditional — the parent handles tab routing.

**ControlsTab.tsx:**

Create `src/client/components/ControlsTab.tsx` exporting `ControlsTab` component.

Props interface `ControlsTabProps`:
- `agents: RegistryAgent[]` — for agent name dropdown options
- `activeInstances: AgentInstance[]` — for target session dropdown options

Imports from `gsdShared.tsx`: `BashHint`.
Imports from `./SearchableSelect.js`: `SearchableSelect`.
Imports from `@shared/gsdTypes.js`: `RegistryAgent`.
Imports from `@shared/types.js`: `AgentInstance`.

Body: Move ALL Controls tab state and logic from GsdView.tsx into ControlsTab:
- The `GSD_COMMANDS` constant (move it here since only Controls uses it)
- All spawn form state: `agentName`, `workdir`, `firstCommand`, `isSpawning`, `spawnStatus`
- All dispatch state: `targetSession`, `commandText`, `isDispatching`, `dispatchStatus`
- The `useEffect` that defaults `targetSession` to first active instance
- The `handleSpawn` callback
- The `handleDispatch` callback
- The `spawnBashCommand` and `dispatchBashCommand` derived strings
- The entire Controls tab JSX (spawn form + dispatch form with SearchableSelect dropdowns and BashHint)

This means ControlsTab is fully self-contained with its own useState/useCallback/useEffect — GsdView passes only data props, no form state.
  </action>
  <verify>
Run `npm run typecheck` — both new files compile. Verify exports: `grep "export function AgentsTab" src/client/components/AgentsTab.tsx` and `grep "export function ControlsTab" src/client/components/ControlsTab.tsx` each return one match.
  </verify>
  <done>
AgentsTab.tsx and ControlsTab.tsx exist as standalone components with correct props, imports, and JSX extracted from GsdView.tsx. Both pass typecheck.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract RegistryTab and HooksTab, then slim GsdView to router shell</name>
  <files>src/client/components/RegistryTab.tsx, src/client/components/HooksTab.tsx, src/client/components/GsdView.tsx</files>
  <action>
**RegistryTab.tsx:**

Create `src/client/components/RegistryTab.tsx` exporting `RegistryTab` component.

Props interface `RegistryTabProps`:
- `registry: GsdRegistry | null` — full registry object
- `registryLoading: boolean`
- `registryError: string | null`
- `getEffectiveEnabled: (agentId: string, serverEnabled: boolean) => boolean`
- `toggleEnabled: (agentId: string, currentEnabled: boolean) => void`

Imports from `@shared/gsdTypes.js`: `GsdRegistry`.

Body: Move the entire `{activeTab === 'registry' && (...)}` JSX block from GsdView.tsx — the loading/error states and registry table with agent rows showing ID, enabled toggle, working dir, session, auto wake, launch mode.

**HooksTab.tsx:**

Create `src/client/components/HooksTab.tsx` exporting `HooksTab` component.

Props interface `HooksTabProps`:
- `hookEvents: Array<{ timestamp: string; hookScript: string; hookEventName: string; agentId?: string; tmuxSession?: string; state?: string }>` — from useGsdHookFeed

Body: Move the entire `{activeTab === 'hooks' && (...)}` JSX block from GsdView.tsx — the empty state message and hook events table with time, hook, event, agent, session, state columns.

**GsdView.tsx rewrite:**

After extracting all 4 tabs, rewrite GsdView.tsx to be a thin router shell:

1. Imports: React (useState, useMemo), the 4 tab components, all data hooks (useGsdRegistry, useGsdHookFeed, useActiveInstances, useAgentLiveStatus, useAgentStateFiles).

2. Keep the `TabId` type and `TABS` constant.

3. `GsdView` function body:
   - `useState<TabId>('agents')` for tab state
   - Call all 5 data hooks (registry, hookFeed, instances, liveStatus, stateFiles)
   - Derive `sessionNames` and `agents` from registry (same useMemo as before)
   - Derive `activeInstances` from instances filter
   - Render: header (title + registered count + loading/error) + tab bar + tab content area
   - Tab content: conditional render of `<AgentsTab>`, `<ControlsTab>`, `<RegistryTab>`, `<HooksTab>` based on activeTab
   - Pass props down to each tab component

4. Delete ALL extracted code: color map imports (now in tab files via gsdShared), GSD_COMMANDS, all spawn/dispatch state, all form handlers, all tab JSX blocks.

5. Target: under 100 lines total.

The header section (title + registered count + loading spinner + error + tab bar) stays in GsdView.tsx — it is tab routing infrastructure, not tab content.
  </action>
  <verify>
Run `npm run typecheck` — zero errors. Run `npm run build` — succeeds. Count lines:
```bash
wc -l src/client/components/GsdView.tsx
```
Must be under 100. Verify all 4 tab files exist:
```bash
ls src/client/components/{Agents,Controls,Registry,Hooks}Tab.tsx
```
  </verify>
  <done>
RegistryTab.tsx and HooksTab.tsx exist as standalone components. GsdView.tsx is under 100 lines, contains only tab state + data hooks + routing. `npm run typecheck` and `npm run build` both pass clean.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm run build` completes successfully
3. `wc -l src/client/components/GsdView.tsx` outputs a number under 100
4. All 4 tab files exist: `ls src/client/components/{Agents,Controls,Registry,Hooks}Tab.tsx`
5. Each tab file exports exactly one named component: AgentsTab, ControlsTab, RegistryTab, HooksTab
6. GsdView.tsx imports and renders all 4 tab components
</verification>

<success_criteria>
- AgentsTab.tsx, ControlsTab.tsx, RegistryTab.tsx, HooksTab.tsx each exist as standalone exports
- GsdView.tsx is under 100 lines with only tab state + routing
- typecheck and build pass clean
- No functional changes to any GSD tab behavior
</success_criteria>

<output>
After completion, create `.planning/phases/16-dry-srp/16-02-SUMMARY.md`
</output>
