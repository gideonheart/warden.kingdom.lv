---
phase: 16-dry-srp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/gsdShared.tsx
  - src/client/components/GsdView.tsx
  - src/client/components/InstanceTabBar.tsx
autonomous: true
requirements:
  - DRY-01
  - DRY-02
  - DRY-03

must_haves:
  truths:
    - "STATUS_COLORS, STATE_BADGE_COLORS, STATE_LABELS, PRESSURE_COLORS are each defined in exactly one file (gsdShared.tsx)"
    - "StateBadge, PressureIndicator, PhaseProgress, CopyButton, BashHint are each defined in exactly one file (gsdShared.tsx)"
    - "GsdView.tsx imports all constants and helper components from gsdShared.tsx — no local definitions remain"
    - "InstanceTabBar.tsx imports STATUS_COLORS from gsdShared.tsx — no local definition remains"
    - "All existing GSD UI behaviors render identically after extraction"
  artifacts:
    - path: "src/client/components/gsdShared.tsx"
      provides: "Shared GSD constants and helper components"
      exports: ["STATUS_COLORS", "STATE_BADGE_COLORS", "STATE_LABELS", "PRESSURE_COLORS", "StateBadge", "PressureIndicator", "PhaseProgress", "CopyButton", "BashHint"]
  key_links:
    - from: "src/client/components/GsdView.tsx"
      to: "src/client/components/gsdShared.tsx"
      via: "named imports"
      pattern: "import.*from.*gsdShared"
    - from: "src/client/components/InstanceTabBar.tsx"
      to: "src/client/components/gsdShared.tsx"
      via: "named import of STATUS_COLORS"
      pattern: "import.*STATUS_COLORS.*from.*gsdShared"
---

<objective>
Extract all duplicated GSD UI constants and helper components into a single shared module, then update every consumer to import from that module.

Purpose: Eliminate duplication of STATUS_COLORS (defined in both GsdView.tsx and InstanceTabBar.tsx) and consolidate the 4 color maps + 5 helper components that will be consumed by the tab sub-components created in Plan 02.

Output: `src/client/components/gsdShared.tsx` with all shared exports; updated imports in GsdView.tsx and InstanceTabBar.tsx.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/client/components/GsdView.tsx
@src/client/components/InstanceTabBar.tsx
@src/shared/gsdTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gsdShared.tsx with extracted constants and components</name>
  <files>src/client/components/gsdShared.tsx</files>
  <action>
Create `src/client/components/gsdShared.tsx` containing:

1. Import `useState`, `useCallback` from React and `AgentStateHint`, `PressureLevel` from `@shared/gsdTypes.js`.

2. Export the 4 color-map constants (copy exactly from GsdView.tsx lines 14-41):
   - `STATUS_COLORS: Record<string, string>` — maps active/idle/stopped/error to warden CSS classes
   - `STATE_BADGE_COLORS: Record<AgentStateHint, string>` — maps state hints to badge color classes
   - `STATE_LABELS: Record<AgentStateHint, string>` — maps state hints to display labels
   - `PRESSURE_COLORS: Record<PressureLevel, string>` — maps pressure levels to text color classes

3. Export the 5 helper components (copy exactly from GsdView.tsx lines 70-144):
   - `StateBadge({ state: AgentStateHint | null })` — renders colored state badge or dash
   - `PressureIndicator({ percentage: number | null, level: PressureLevel | null })` — renders percentage with color
   - `PhaseProgress({ phase: string | null, progress: number | null })` — renders "P{n} {pct}%"
   - `CopyButton({ text: string })` — clipboard copy with "Copied!" feedback (uses useState + useCallback)
   - `BashHint({ command: string })` — inline code block with CopyButton

All JSX, class names, and behavior must be identical to the originals. No functional changes.
  </action>
  <verify>
Run `npm run typecheck` — gsdShared.tsx compiles with no errors. Visually inspect that all 4 constants and 5 components are exported.
  </verify>
  <done>
gsdShared.tsx exists, exports 9 named symbols (4 constants + 5 components), and passes typecheck.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GsdView.tsx and InstanceTabBar.tsx to import from gsdShared</name>
  <files>src/client/components/GsdView.tsx, src/client/components/InstanceTabBar.tsx</files>
  <action>
**GsdView.tsx changes:**

1. Add import at the top:
   ```ts
   import { STATUS_COLORS, STATE_BADGE_COLORS, STATE_LABELS, PRESSURE_COLORS, StateBadge, PressureIndicator, PhaseProgress, CopyButton, BashHint } from './gsdShared.js';
   ```

2. Delete the local constant definitions (lines 14-41 — the 4 color maps: STATUS_COLORS, STATE_BADGE_COLORS, STATE_LABELS, PRESSURE_COLORS).

3. Delete the local helper component definitions (lines 70-144 — StateBadge, PressureIndicator, PhaseProgress, CopyButton, BashHint).

4. Delete the "Color maps" and "Helper components" section comment headers that wrapped the deleted code.

5. The `AgentStateHint` and `PressureLevel` type imports from `@shared/gsdTypes.js` can be removed from GsdView.tsx ONLY IF no other code in GsdView.tsx references them directly. Check: the agent card section uses `agentStatus?.state` and `agentStatus?.contextPressureLevel` but those are inferred through the hook return types, not directly annotated with these types — so the type import can be safely removed.

**InstanceTabBar.tsx changes:**

1. Add import:
   ```ts
   import { STATUS_COLORS } from './gsdShared.js';
   ```

2. Delete the local `STATUS_COLORS` constant (lines 11-16).

All usages in both files remain unchanged — they already reference these symbols by name.
  </action>
  <verify>
Run `npm run typecheck` — zero errors. Run `npm run build` — builds successfully. Grep for duplicate definitions:
```bash
grep -rn "const STATUS_COLORS" src/client/components/
grep -rn "const STATE_BADGE_COLORS" src/client/components/
grep -rn "function StateBadge" src/client/components/
```
Each should appear exactly once (in gsdShared.tsx only).
  </verify>
  <done>
GsdView.tsx and InstanceTabBar.tsx import from gsdShared.tsx. No duplicate constant or component definitions exist anywhere in src/client/components/. `npm run typecheck` and `npm run build` pass.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm run build` completes successfully
3. `grep -rn "const STATUS_COLORS" src/client/` returns exactly 1 match (in gsdShared.tsx)
4. `grep -rn "const STATE_BADGE_COLORS" src/client/` returns exactly 1 match (in gsdShared.tsx)
5. `grep -rn "function StateBadge" src/client/` returns exactly 1 match (in gsdShared.tsx)
6. `grep -rn "function CopyButton" src/client/` returns exactly 1 match (in gsdShared.tsx)
</verification>

<success_criteria>
- gsdShared.tsx exports all 9 symbols (4 constants + 5 components)
- Zero duplicate definitions of any extracted symbol in src/client/
- GsdView.tsx and InstanceTabBar.tsx import from gsdShared.tsx
- typecheck and build pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/16-dry-srp/16-01-SUMMARY.md`
</output>
