---
phase: 26-token-analytics-polish-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/TokenUsageView.tsx
  - src/client/components/ModelComparisonView.tsx
  - src/server/routes/instanceRoutes.ts
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Agent filter input is visible and functional on both the Token Usage tab and the Model Costs tab without switching tabs"
    - "Changing the agent filter while on the Model Costs tab updates ModelComparisonView immediately"
    - "Flow C (Token usage → Model Costs tab → change agent filter → Export CSV) completes end to end"
    - "The '24h' time range in ModelComparisonView is labelled 'Today' and reflects today's data (calendar day), consistent with its implementation"
    - "instanceRoutes.ts has no unused imports (openSync/closeSync already cleaned up in quick-2039)"
  artifacts:
    - path: "src/client/components/TokenUsageView.tsx"
      provides: "agentFilter input in shared header visible on both tabs; filter passed to ModelComparisonView"
      contains: "agentFilter"
    - path: "src/client/components/ModelComparisonView.tsx"
      provides: "TIME_RANGE_LABELS updated: '24h' label changed to 'Today'"
      contains: "Today"
  key_links:
    - from: "src/client/components/TokenUsageView.tsx"
      to: "src/client/components/ModelComparisonView.tsx"
      via: "agentFilter prop"
      pattern: "agentFilter.*ModelComparisonView"
    - from: "agentFilter state in TokenUsageView"
      to: "ModelComparisonView fetch"
      via: "agentFilter prop triggers useEffect re-fetch"
      pattern: "agentFilter.*useEffect"
---

<objective>
Fix two UX gaps and one label semantic issue introduced in Phases 21–23:

1. Agent filter input is hidden when the Model Costs tab is active (it lives inside the Token Usage tab block). Move it to the shared header row so operators can filter by agent on any tab.
2. The `24h` label in ModelComparisonView is semantically misleading — the implementation uses today's calendar date as `dateFrom`, which is "today since midnight" not "rolling last 24 hours". Rename label to "Today".
3. Verify the unused `openSync`/`closeSync` import in instanceRoutes.ts is already gone (removed in quick-2039). No action needed if verified clean.

Purpose: Close the Flow C gap so the full sequence (Token Usage → Model Costs → change agent filter → Export CSV) works without tab-switching gymnastics.
Output: Updated TokenUsageView.tsx, ModelComparisonView.tsx. No new files.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/client/components/TokenUsageView.tsx
@src/client/components/ModelComparisonView.tsx
@src/server/routes/instanceRoutes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move agent filter to shared header in TokenUsageView</name>
  <files>src/client/components/TokenUsageView.tsx</files>
  <action>
The `agentFilter` input is currently rendered inside the `{activeTab === 'usage' && (...)}` block (around line 298–321). This hides it when the operator is on the Model Costs tab.

Move the agent filter input out of the `usage` tab block into the shared header row (the flex row containing the tab bar and Export CSV button, around line 253–288).

Specifically:
1. Find the `{/* Filter controls and scan button */}` block inside the `usage` tab section (the `<div className="flex items-center gap-3">` containing the agentFilter `<input>` and Scan Now button).
2. Remove that entire `<div>` from inside the `usage` block.
3. Add the agentFilter input to the shared header row. The simplest placement is between the tab bar buttons and the Export CSV button. Add the input inline in the header flex row:

```tsx
{/* Agent filter — visible on all tabs */}
<input
  type="text"
  placeholder="Filter by agent"
  value={agentFilter}
  onChange={(e) => setAgentFilter(e.target.value)}
  className="bg-warden-bg border border-warden-border rounded px-2 py-1 text-sm text-warden-text w-36"
/>
```

Place this input between the tab buttons div and the Export CSV button in the header flex row.

The Scan Now button can remain inside the `usage` tab block since it is only relevant for the token usage table. Only the agentFilter input needs to move to the shared header.

Keep the `agentFilter` state definition unchanged (line ~62: `const [agentFilter, setAgentFilter] = useState('');`). The `agentFilter` prop already flows to `ModelComparisonView` at line 292, so once the input is in the shared header, filtering will work on both tabs.

Do NOT change any other logic — `fetchUsage` already depends on `agentFilter` and re-fetches on change.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build 2>&1 | tail -20</automated>
    <manual>Open http://localhost:5173 → History → Token Usage. Confirm the "Filter by agent" input is visible in the header row. Switch to the Model Costs tab — confirm the filter input is still visible. Type a filter value while on Model Costs tab — confirm the bar chart updates to show only matching agents.</manual>
  </verify>
  <done>Agent filter input is in the shared header row visible on both tabs; typing a value while on the Model Costs tab filters the ModelComparisonView bar chart.</done>
</task>

<task type="auto">
  <name>Task 2: Rename '24h' time range label to 'Today' in ModelComparisonView and verify instanceRoutes.ts</name>
  <files>src/client/components/ModelComparisonView.tsx</files>
  <action>
**Part A — Fix TIME_RANGE_LABELS in ModelComparisonView.tsx:**

The `calculateDateFrom` function for the `'24h'` range uses today's calendar date (`now.toISOString().slice(0, 10)`), which is "today since midnight" — not a rolling 24-hour window. The label should reflect the actual semantics.

In `ModelComparisonView.tsx`, change the `TIME_RANGE_LABELS` map entry:

```ts
// Before:
const TIME_RANGE_LABELS: Record<TimeRange, string> = {
  '24h': '24h',
  ...
};

// After:
const TIME_RANGE_LABELS: Record<TimeRange, string> = {
  '24h': 'Today',
  ...
};
```

Only the label changes. The `TimeRange` type key `'24h'`, the `calculateDateFrom` logic, and all other code remain unchanged. This is a one-line label string change.

**Part B — Verify instanceRoutes.ts has no unused imports:**

Read `src/server/routes/instanceRoutes.ts` lines 1–7 and confirm there is no `import { openSync, closeSync } from 'fs'` line. This was removed in quick-2039 (commit 0adb236). If the import is absent, no action is needed — note the verification in the task output. If somehow it is still present (regression), remove it.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build 2>&1 | tail -20 && grep -n "openSync\|closeSync" src/server/routes/instanceRoutes.ts || echo "CLEAN: no unused fs imports in instanceRoutes.ts"</automated>
    <manual>Open Model Costs tab in the History view. Confirm the first time range button now reads "Today" instead of "24h". Confirm other labels (7d, 30d, All) are unchanged.</manual>
  </verify>
  <done>"Today" label appears on the first time range button in ModelComparisonView. `instanceRoutes.ts` has no `openSync`/`closeSync` import. Build passes with zero TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` passes with zero TypeScript errors
2. Agent filter input is in the shared header, visible on both Token Usage and Model Costs tabs
3. Flow C: open History → Token Usage → switch to Model Costs → type in filter → chart updates → click Export CSV → file downloads — all without switching tabs
4. Model Costs time range selector shows "Today | 7d | 30d | All" (not "24h | 7d | 30d | All")
5. `grep -n "openSync\|closeSync" src/server/routes/instanceRoutes.ts` returns no matches
</verification>

<success_criteria>
- Agent filter input is visible on both Token Usage and Model Costs tabs without any tab-switching
- Flow C completes end to end: navigate to Model Costs → change agent filter → Export CSV triggers download
- "Today" label on ModelComparisonView's first time range button; implementation unchanged
- instanceRoutes.ts is clean (verified no openSync/closeSync — already removed in quick-2039)
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/26-token-analytics-polish-tech-debt/26-01-SUMMARY.md`
</output>
