---
phase: 35-budget-alerts-notification-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/server/database/DatabaseConnection.ts
  - src/server/services/BudgetAlertPoller.ts
  - src/server/services/NotificationDeduplicator.ts
  - tests/unit/BudgetAlertPoller.test.ts
  - tests/unit/NotificationDeduplicator.test.ts
autonomous: true
requirements:
  - BUDG-01
  - BUDG-02
  - NSET-03

must_haves:
  truths:
    - "BudgetAlertPoller fires a Telegram message when an agent crosses amber (80%) threshold"
    - "BudgetAlertPoller fires a separate Telegram message when an agent crosses red (100%) threshold"
    - "Sustained threshold produces at most one alert per cooldown window (no spam)"
    - "Return to 'ok' resets dedup state so next breach fires fresh"
    - "Warning-to-exceeded transition fires an escalation alert immediately"
    - "notification_config table persists preferences and returns sensible defaults when no row exists"
    - "NotificationDeduplicator accepts configurable cooldown instead of hardcoded constant"
  artifacts:
    - path: "src/server/services/BudgetAlertPoller.ts"
      provides: "Budget threshold polling and Telegram alert firing"
      exports: ["BudgetAlertPoller", "budgetAlertPoller"]
    - path: "src/shared/types.ts"
      provides: "NotificationConfig shared type"
      contains: "interface NotificationConfig"
    - path: "src/server/database/DatabaseConnection.ts"
      provides: "notification_config migration, getNotificationConfig(), setNotificationConfig()"
      contains: "notification_config"
    - path: "tests/unit/BudgetAlertPoller.test.ts"
      provides: "Unit tests for budget alert deduplication and Telegram firing"
      min_lines: 50
    - path: "tests/unit/NotificationDeduplicator.test.ts"
      provides: "Updated tests with cooldownMs parameter"
      min_lines: 80
  key_links:
    - from: "src/server/services/BudgetAlertPoller.ts"
      to: "src/server/database/DatabaseConnection.ts"
      via: "database.getBudgetAlertStatus() and database.getNotificationConfig()"
      pattern: "database\\.get(BudgetAlertStatus|NotificationConfig)"
    - from: "src/server/services/BudgetAlertPoller.ts"
      to: "src/server/services/TelegramBotService.ts"
      via: "telegramBotService.sendToTopic()"
      pattern: "telegramBotService\\.sendToTopic"
    - from: "src/server/services/NotificationDeduplicator.ts"
      to: "recordAndCheck cooldownMs parameter"
      via: "third parameter replaces hardcoded PERMISSION_COOLDOWN_MS"
      pattern: "recordAndCheck.*cooldownMs"
---

<objective>
Build BudgetAlertPoller service, notification_config database persistence, and refactor NotificationDeduplicator to accept configurable cooldown.

Purpose: Enable server-side budget threshold monitoring that fires Telegram alerts on amber/red breaches with proper deduplication, and persist notification preferences in SQLite so they survive restarts.

Output: BudgetAlertPoller singleton service, notification_config table with get/set methods, NotificationConfig shared type, NotificationDeduplicator with cooldownMs parameter, comprehensive unit tests.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-budget-alerts-notification-settings/35-RESEARCH.md
@src/server/services/NotificationPoller.ts
@src/server/services/NotificationDeduplicator.ts
@src/server/services/TelegramBotService.ts
@src/server/database/DatabaseConnection.ts
@src/shared/types.ts
@tests/unit/NotificationDeduplicator.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NotificationConfig type, DB migration, and DB methods</name>
  <files>
    src/shared/types.ts
    src/server/database/DatabaseConnection.ts
  </files>
  <action>
1. In `src/shared/types.ts`, add the `NotificationConfig` interface after the `RotationConfig` interface:
```typescript
export interface NotificationConfig {
  permissionAlertsEnabled: boolean;
  budgetAlertsEnabled: boolean;
  permissionCooldownMs: number;
  budgetCooldownMs: number;
}
```

2. In `src/server/database/DatabaseConnection.ts`:
   - Add `NotificationConfig` to the import from `../../shared/types.js`
   - Add a new migration at the end of `runMigrations()` (after the `deletion_pending` migration):
```sql
CREATE TABLE IF NOT EXISTS notification_config (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  permission_alerts_enabled INTEGER NOT NULL DEFAULT 1,
  budget_alerts_enabled INTEGER NOT NULL DEFAULT 1,
  permission_cooldown_ms INTEGER NOT NULL DEFAULT 120000,
  budget_cooldown_ms INTEGER NOT NULL DEFAULT 600000,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
)
```
   - Add `getNotificationConfig(): NotificationConfig` method (after `setRotationConfig`). Use the `?? defaults` pattern matching `getRotationConfig()` — query `notification_config WHERE id = 1`, return defaults if no row:
     - `permissionAlertsEnabled: true`, `budgetAlertsEnabled: true`, `permissionCooldownMs: 120000`, `budgetCooldownMs: 600000`
   - Add `setNotificationConfig(config: Partial<NotificationConfig>): void` method. Read current config, merge, then `INSERT ... ON CONFLICT(id) DO UPDATE SET` (copy the `setRotationConfig` pattern exactly, but with 4 columns).
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npx tsc --noEmit</automated>
    <manual>Verify notification_config table migration is idempotent (CREATE TABLE IF NOT EXISTS)</manual>
  </verify>
  <done>NotificationConfig type exported from shared types. getNotificationConfig() returns defaults when no row exists. setNotificationConfig() upserts with ON CONFLICT pattern. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create BudgetAlertPoller service and refactor NotificationDeduplicator cooldown</name>
  <files>
    src/server/services/BudgetAlertPoller.ts
    src/server/services/NotificationDeduplicator.ts
    tests/unit/BudgetAlertPoller.test.ts
    tests/unit/NotificationDeduplicator.test.ts
  </files>
  <action>
1. **Refactor NotificationDeduplicator** (`src/server/services/NotificationDeduplicator.ts`):
   - Remove the `PERMISSION_COOLDOWN_MS` module-level constant
   - Change `recordAndCheck(sessionName: string, state: AgentStateHint)` signature to `recordAndCheck(sessionName: string, state: AgentStateHint, cooldownMs: number)`
   - Replace the hardcoded `PERMISSION_COOLDOWN_MS` reference in the cooldown check with the `cooldownMs` parameter
   - Everything else stays the same

2. **Update NotificationDeduplicator tests** (`tests/unit/NotificationDeduplicator.test.ts`):
   - Every existing `deduplicator.recordAndCheck('session-x', state)` call must add a third argument: `120_000` (the previous default)
   - Add one new test: "respects custom cooldown value" — create deduplicator, fire permission_prompt with cooldownMs=300000 (5 min), advance time 3 min, re-enter (should suppress), advance to 5.5 min (should fire)
   - All existing test semantics remain identical — only adding the cooldownMs arg

3. **Create BudgetAlertPoller** (`src/server/services/BudgetAlertPoller.ts`):
   - Follow the NotificationPoller pattern exactly: class with `startPolling()` / `stopPolling()`, exported singleton `budgetAlertPoller`
   - Private `records = new Map<string, { level: BudgetLevel; lastAlertedAt: number | null }>()` where `BudgetLevel = 'ok' | 'warning' | 'exceeded'`
   - `POLL_INTERVAL_MS = 10_000`
   - `pollBudgets()`: read `database.getNotificationConfig()` — if `!config.budgetAlertsEnabled`, return early. Then call `database.getBudgetAlertStatus()` and process each status via `checkAgent(status, config.budgetCooldownMs)`
   - `checkAgent(status, cooldownMs)`: if level === 'ok', delete record and return. Otherwise check for escalation (ok->warning, ok->exceeded, warning->exceeded) OR cooldown expiry. If neither, suppress. If either, update record and call `sendBudgetAlert()`.
   - `isLevelEscalation(previous, current)`: use rank map `{ ok: 0, warning: 1, exceeded: 2 }`, return `rank[current] > rank[previous]`
   - `sendBudgetAlert(agentId, level, budgetPct, todayCostUsd, dailyBudgetUsd)`: look up topic mapping via `openClawConfigReader.getTopicMappings()`, format message with distinct emoji (yellow circle for warning, red circle for exceeded), send via `telegramBotService.sendToTopic()`. Use Markdown parse_mode formatting: `*Budget WARNING*` vs `*BUDGET EXCEEDED*`.
   - Import: `database`, `telegramBotService`, `openClawConfigReader` from their respective modules

4. **Create BudgetAlertPoller tests** (`tests/unit/BudgetAlertPoller.test.ts`):
   - Mock `database` (getBudgetAlertStatus, getNotificationConfig), `telegramBotService` (sendToTopic, isRunning), `openClawConfigReader` (getTopicMappings)
   - Use `vi.useFakeTimers()` for cooldown testing
   - Test cases:
     a. Fires amber alert when agent at 85% (BUDG-01)
     b. Fires red alert when agent at 105% with distinct formatting (BUDG-01)
     c. Does NOT fire when budgetAlertsEnabled is false (NSET-01)
     d. Suppresses repeated alerts within cooldown window (BUDG-02)
     e. Fires again after cooldown expires (BUDG-02)
     f. Fires immediately on escalation warning->exceeded even within cooldown (BUDG-02)
     g. Resets state when agent returns to 'ok' — next warning fires fresh
     h. Does nothing when no topic mapping exists for agentId
   - Use `@vitest-environment node` pragma at top
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run test</automated>
    <manual>Check that all existing NotificationDeduplicator tests still pass with the new cooldownMs parameter</manual>
  </verify>
  <done>BudgetAlertPoller class exists with startPolling/stopPolling and deduplication logic. NotificationDeduplicator.recordAndCheck accepts cooldownMs parameter. All unit tests pass including new BudgetAlertPoller tests and updated NotificationDeduplicator tests.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles without errors
2. `npm run test` — all unit tests pass (existing + new BudgetAlertPoller + updated NotificationDeduplicator)
3. NotificationConfig type is importable from `@shared/types.js`
4. BudgetAlertPoller exports a singleton instance
5. NotificationDeduplicator no longer has PERMISSION_COOLDOWN_MS constant
</verification>

<success_criteria>
- NotificationConfig interface exported from shared types
- notification_config table migration added to DatabaseConnection
- getNotificationConfig() returns defaults when table is empty
- setNotificationConfig() upserts correctly
- BudgetAlertPoller fires distinct amber/red alerts via TelegramBotService
- BudgetAlertPoller suppresses alerts within cooldown, fires on escalation
- NotificationDeduplicator accepts cooldownMs as parameter (no hardcoded constant)
- All tests green (npm run test)
</success_criteria>

<output>
After completion, create `.planning/phases/35-budget-alerts-notification-settings/35-01-SUMMARY.md`
</output>
