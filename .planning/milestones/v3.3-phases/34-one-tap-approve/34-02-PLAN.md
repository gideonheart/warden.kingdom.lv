---
phase: 34-one-tap-approve
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/server/services/TelegramBotService.ts
  - src/server/services/NotificationPoller.ts
  - src/server/index.ts
  - tests/unit/TelegramBotService.test.ts
autonomous: true
requirements:
  - APRV-01
  - APRV-02
  - APRV-04

must_haves:
  truths:
    - "Permission notifications include an Approve inline keyboard button"
    - "Tapping Approve sends input to the agent's tmux session and unblocks it"
    - "Approved message is edited to show 'Approved at HH:MM' with button removed"
    - "ApprovalStateTracker is pruned on each poll cycle"
    - "ApprovalCallbackHandler is registered on the bot before bot.start()"
  artifacts:
    - path: "src/server/services/TelegramBotService.ts"
      provides: "sendToTopicWithApproveButton() returning message_id, registerCallbackHandler() for pre-start registration"
      exports: ["TelegramBotService", "telegramBotService"]
    - path: "src/server/services/NotificationPoller.ts"
      provides: "Updated notification pipeline calling sendToTopicWithApproveButton and registering approvals"
      contains: "sendToTopicWithApproveButton"
    - path: "src/server/index.ts"
      provides: "ApprovalCallbackHandler wired into server startup"
      contains: "ApprovalCallbackHandler"
  key_links:
    - from: "src/server/services/NotificationPoller.ts"
      to: "src/server/services/TelegramBotService.ts"
      via: "sendToTopicWithApproveButton"
      pattern: "sendToTopicWithApproveButton"
    - from: "src/server/services/NotificationPoller.ts"
      to: "src/server/services/ApprovalStateTracker.ts"
      via: "approvalStateTracker.register"
      pattern: "approvalStateTracker\\.register"
    - from: "src/server/index.ts"
      to: "src/server/services/ApprovalCallbackHandler.ts"
      via: "ApprovalCallbackHandler constructor and registerCallbackHandler"
      pattern: "ApprovalCallbackHandler|registerCallbackHandler"
---

<objective>
Wire the approval button into the notification pipeline and register the callback handler on the bot.

Purpose: Connect the ApprovalStateTracker and ApprovalCallbackHandler (built in Plan 01) to the existing TelegramBotService and NotificationPoller, completing the end-to-end one-tap approve flow from notification to tmux input injection.

Output: Permission notifications now include an Approve button; tapping it sends `1` to the agent's tmux session and edits the message.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-one-tap-approve/34-RESEARCH.md
@.planning/phases/34-one-tap-approve/34-01-SUMMARY.md
@src/server/services/TelegramBotService.ts
@src/server/services/NotificationPoller.ts
@src/server/services/ApprovalStateTracker.ts
@src/server/services/ApprovalCallbackHandler.ts
@src/server/index.ts
@tests/unit/TelegramBotService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sendToTopicWithApproveButton and registerCallbackHandler to TelegramBotService</name>
  <files>
    src/server/services/TelegramBotService.ts
    tests/unit/TelegramBotService.test.ts
  </files>
  <action>
**Extend TelegramBotService with two new methods:**

1. `sendToTopicWithApproveButton(chatId: string, topicId: string, text: string, sessionName: string): Promise<number | null>`

   - Guard: if `!this.bot`, log warning and return `null`
   - Import `InlineKeyboard` from `grammy`
   - Create keyboard: `new InlineKeyboard().text('Approve', \`approve:\${sessionName}\`)`
   - Call `this.bot.api.sendMessage(chatId, text, { message_thread_id: parseInt(topicId, 10), parse_mode: 'Markdown', reply_markup: keyboard })`
   - Return `sentMessage.message_id` on success, `null` on error (catch and log)

2. `registerCallbackHandler(handler: (bot: Bot) => void): void`

   - Stores the handler in a private `callbackHandlers: Array<(bot: Bot) => void>` array
   - Must be called BEFORE `start()` — handlers are applied during `start()` after `new Bot(token)` but BEFORE `bot.start()`

**Modify `start()` method:**
   - After creating `this.bot = new Bot(token)` and registering autoRetry and catch handler, but BEFORE calling `this.bot.start(...)`:
   - Iterate `this.callbackHandlers` and call each with `this.bot`
   - This ensures callback query handlers are registered before long polling begins

**Add tests to `tests/unit/TelegramBotService.test.ts`:**

1. `APRV-01: sendToTopicWithApproveButton sends message with InlineKeyboard reply_markup` — mock bot.api.sendMessage, verify it receives reply_markup with InlineKeyboard, verify message_id is returned
2. `sendToTopicWithApproveButton returns null when bot is not running` — don't start bot, call method, verify returns null
3. `sendToTopicWithApproveButton returns null on send error` — mock sendMessage to reject, verify returns null (error caught)
4. `registerCallbackHandler applies handlers during start()` — register a spy handler before start(), verify it receives the bot instance during start()

Commit: `feat(34-02): add sendToTopicWithApproveButton and registerCallbackHandler to TelegramBotService`
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npx vitest run tests/unit/TelegramBotService.test.ts 2>&1 | tail -10</automated>
    <manual>All TelegramBotService tests pass including new APRV-01 tests</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>TelegramBotService has sendToTopicWithApproveButton returning message_id and registerCallbackHandler. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire NotificationPoller to use approve button and register handler in server startup</name>
  <files>
    src/server/services/NotificationPoller.ts
    src/server/index.ts
  </files>
  <action>
**Update NotificationPoller (`src/server/services/NotificationPoller.ts`):**

1. Import `approvalStateTracker` from `./ApprovalStateTracker.js`
2. In `sendPermissionNotification()`:
   - Change `telegramBotService.sendToTopic(...)` to `telegramBotService.sendToTopicWithApproveButton(mapping.groupId, mapping.topicId, text, sessionName)`
   - Store the returned `messageId` — if not null, call `approvalStateTracker.register(sessionName, { chatId: mapping.groupId, messageId, topicId: mapping.topicId, originalText: text })`
3. In `pollAllSessions()`:
   - Call `approvalStateTracker.pruneExpired()` at the start of each poll cycle (free housekeeping, no extra timer)

**Update server startup (`src/server/index.ts`):**

1. Import `ApprovalCallbackHandler` from `./services/ApprovalCallbackHandler.js`
2. Import `approvalStateTracker` from `./services/ApprovalStateTracker.js`
3. Import `tmuxSessionManager` from `./services/TmuxSessionManager.js` (if not already imported)
4. After creating all service instances but BEFORE `telegramBotService.start()`:
   - Create `const approvalCallbackHandler = new ApprovalCallbackHandler(approvalStateTracker, tmuxSessionManager)`
   - Call `telegramBotService.registerCallbackHandler((bot) => approvalCallbackHandler.register(bot))`
5. No shutdown changes needed — ApprovalCallbackHandler has no resources to clean up; ApprovalStateTracker is in-memory

**Verify full pipeline:**
- `npm run test` — all tests pass
- `npm run typecheck` — clean
- `npm run build` — production build succeeds

Commit: `feat(34-02): wire approve button into notification pipeline and register callback handler`
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run test 2>&1 | tail -5 && npm run typecheck 2>&1 | tail -3 && npm run build 2>&1 | tail -3</automated>
    <manual>All tests pass, typecheck clean, production build succeeds</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>NotificationPoller sends notifications with approve button and registers approvals. ApprovalCallbackHandler registered on bot before start. Full test suite passes. TypeScript clean. Production build succeeds.</done>
</task>

</tasks>

<verification>
```bash
# All tests pass
npm run test

# TypeScript typecheck
npm run typecheck

# Production build
npm run build

# Verify wiring in built output
grep 'sendToTopicWithApproveButton' dist/server/server/services/NotificationPoller.js
grep 'ApprovalCallbackHandler' dist/server/server/index.js
grep 'registerCallbackHandler' dist/server/server/services/TelegramBotService.js
grep 'InlineKeyboard' dist/server/server/services/TelegramBotService.js
grep 'approvalStateTracker' dist/server/server/services/NotificationPoller.js
```
</verification>

<success_criteria>
- Permission notifications include InlineKeyboard with Approve button (APRV-01)
- Callback handler registered on bot before start, processes approve taps (APRV-02)
- Message edited to remove button after approval (APRV-04)
- ApprovalStateTracker pruned every poll cycle
- All tests pass, typecheck clean, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/34-one-tap-approve/34-02-SUMMARY.md`
</output>
