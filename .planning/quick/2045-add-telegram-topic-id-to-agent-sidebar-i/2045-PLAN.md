---
phase: quick
plan: 2045
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/AgentSidebar.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Each agent row in the sidebar shows its Telegram topic ID (e.g. #41) inline next to the agent name"
    - "Agents without a topic mapping show no topic label"
    - "The topic label is visually subtle — small text, dimmed color, does not crowd the agent name"
  artifacts:
    - path: "src/client/components/AgentSidebar.tsx"
      provides: "Inline topic ID label per agent row"
      contains: "topicId"
  key_links:
    - from: "AgentSidebar agent list loop"
      to: "topicMappings prop"
      via: "Array.find lookup by agent.id"
      pattern: "topicMappings\\.find.*agent\\.id"
---

<objective>
Add the Telegram topic ID as a small inline label next to each agent's name in the AgentSidebar list.

Purpose: Operators can immediately see which Telegram topic each agent maps to without clicking into the details panel.
Output: Updated AgentSidebar.tsx with topic ID labels in agent rows.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/client/components/AgentSidebar.tsx
@src/shared/openclawTypes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline topic ID label to each agent row in AgentSidebar</name>
  <files>src/client/components/AgentSidebar.tsx</files>
  <action>
In the agent list loop (the `agents.map()` block around line 115), look up the first matching topic mapping for each agent and render a small `#topicId` label inline.

Specific changes:

1. Inside the `agents.map((agent) => ...)` block, before the JSX return, find the agent's topic mapping:
   ```
   const agentTopic = topicMappings.find((t) => t.agentId === agent.id);
   ```

2. Inside the button element (the clickable area with agent name, around line 124-133), after the agent name `<span>` and the `isDefault` label, add the topic ID label:
   ```tsx
   {agentTopic && (
     <span className="text-xs text-warden-text-dim/50 flex-shrink-0 font-mono">
       #{agentTopic.topicId}
     </span>
   )}
   ```

   Place this AFTER the `{agent.isDefault && ...}` block so the visual order is:
   [green dot] [agent name] [default badge if applicable] [#topicId]

3. The label styling should be:
   - `text-xs` — small size matching the "default" badge
   - `text-warden-text-dim/50` — subtle/dimmed so it doesn't compete with the agent name
   - `font-mono` — monospace for the number to look like a code/ID reference
   - `flex-shrink-0` — prevent truncation of the topic ID

Do NOT remove the existing "Topic Map" section at the bottom or the "Telegram Topics" in the details panel — those stay as they are.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build</automated>
    <manual>Open Warden in browser, check that each agent in the sidebar shows its topic ID (e.g. #41) next to the name</manual>
  </verify>
  <done>Each agent row in AgentSidebar displays the Telegram topic ID as a small monospace label next to the agent name. Agents without topic mappings show no label. Build passes without errors.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without TypeScript or build errors
- Visual inspection: agent rows show `#topicId` labels inline
</verification>

<success_criteria>
- Topic ID labels appear inline in each agent sidebar row for agents that have topic mappings
- Agents without mappings show no extra label
- Existing Topic Map section and Details panel topic info remain unchanged
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/2045-add-telegram-topic-id-to-agent-sidebar-i/2045-SUMMARY.md`
</output>
