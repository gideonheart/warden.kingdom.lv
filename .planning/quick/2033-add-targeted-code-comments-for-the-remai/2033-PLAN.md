---
phase: quick-2033
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/hooks/useTerminalSocket.ts
  - src/client/hooks/useSessionSelection.ts
autonomous: true
requirements: [RISK-1, RISK-2, RISK-3, EDGE-2]
must_haves:
  truths:
    - "OVERLAY_DELAY_MS JSDoc explains that PTY-exit reconnects (>2s) still trigger the overlay"
    - "Null guard on overlayDelayTimerRef has a comment explaining double-timer prevention"
    - "Ref-in-render-body pattern on lines 55-57 has a comment explaining accepted purity deviation"
    - "selectedSessionName dep exclusion comment explains stale-closure safety (effects run post-commit)"
  artifacts:
    - path: "src/client/hooks/useTerminalSocket.ts"
      provides: "Three new targeted comments for RISK-1, RISK-2, EDGE-2"
    - path: "src/client/hooks/useSessionSelection.ts"
      provides: "Expanded stale-closure safety comment for RISK-3"
  key_links: []
---

<objective>
Add targeted code comments to useTerminalSocket and useSessionSelection addressing the remaining
low-risk documentation items identified in the quick-2030 code review (2030-REVIEW.md follow-ups
#2, #3, #7, and RISK-1).

Purpose: Prevent future maintainers from "fixing" intentional patterns that look like bugs
(overlay delay during PTY exit, stale closure dep exclusion, ref assignment in render body,
double-timer guard). All findings were severity: low with recommendation "document in comment."

Output: Two modified hook files with precise inline comments. No behavioral changes.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/2030-review-debug-fix-commit-6dd185d-identify/2030-REVIEW.md
@src/client/hooks/useTerminalSocket.ts
@src/client/hooks/useSessionSelection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted comments to useTerminalSocket.ts</name>
  <files>src/client/hooks/useTerminalSocket.ts</files>
  <action>
Add three targeted comments to useTerminalSocket.ts. These are documentation-only changes — no code modifications.

1. **RISK-2 — OVERLAY_DELAY_MS and PTY-exit interaction** (near the existing JSDoc on lines 15-20):
   Expand the existing JSDoc on `OVERLAY_DELAY_MS` to note that PTY-exit reconnects take >2s
   (socket.disconnect + 2000ms setTimeout + new socket connect) which exceeds the 500ms threshold,
   so the overlay WILL appear for ~1.5s during PTY-exit cycles. This is intentional and correct:
   the overlay should show when a genuinely new PTY is being spawned. The 500ms delay only
   suppresses flashes for sub-500ms network hiccups where Socket.IO auto-reconnects quickly.

2. **RISK-1 — Ref assignment in render body** (above lines 55-57 where onTerminalOutputRef.current
   is assigned):
   Add a brief comment noting this is an intentional deviation from React's render purity rule.
   Concurrent mode may discard and replay renders, causing these assignments to run for uncommitted
   renders, but this is benign — the ref simply receives the same or newer callback. The pattern is
   widely used as a pragmatic escape hatch (see React docs on refs). The strictly correct
   alternative is useLayoutEffect, but the complexity is not justified at this scale.

3. **EDGE-2 — Null guard prevents double-timer** (on the `if (overlayDelayTimerRef.current === null)`
   line, currently line 70):
   Add a comment explaining that this guard ensures at most one overlay delay timer exists at any
   time. If React batches rapid isConnected false->true->false transitions, this prevents a second
   timer from being created while the first is still pending.

Do NOT modify any code logic, only add comments. Keep comments concise (2-4 lines each).
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npx tsc --noEmit --project tsconfig.json 2>&1 | head -5</automated>
    <manual>Verify comments are present at the three locations and no code was changed</manual>
  </verify>
  <done>useTerminalSocket.ts has three new comments addressing RISK-1, RISK-2, and EDGE-2 from the 2030 review. No behavioral changes. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Expand stale-closure safety comment in useSessionSelection.ts</name>
  <files>src/client/hooks/useSessionSelection.ts</files>
  <action>
Expand the existing comment block at lines 120-126 (after the eslint-disable line) to also
explain WHY the stale closure is safe, not just WHAT is excluded.

The existing comment explains that selectedSessionName is excluded so the effect only re-runs on
poll cycles. Expand it to add the safety rationale from RISK-3:

- Effects run AFTER React commits state updates to the DOM, not during the batched render phase.
- When selectSession('B') and a poll arrive in the same React batch, React commits the state
  update from selectSession first, then runs effects. The closure captures the post-commit value
  ('B'), not the pre-selection value ('A').
- Adding selectedSessionName to deps would break hysteresis: every manual selection would re-trigger
  the effect, potentially undoing the selection if the poll list hasn't updated yet.

Keep the expanded comment concise — aim for 6-8 total lines (currently 5 lines). Do NOT change
any code logic.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npx tsc --noEmit --project tsconfig.json 2>&1 | head -5</automated>
    <manual>Verify the expanded comment is present and no code was changed</manual>
  </verify>
  <done>useSessionSelection.ts dep-exclusion comment now explains both WHAT is excluded and WHY the stale closure is safe (effects run post-commit). No behavioral changes. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes (no code changes, only comments)
- `npm run build` succeeds (production build still works)
- Both files have the expected new comments at the specified locations
- No lines of actual code were modified in either file
</verification>

<success_criteria>
All four review findings (RISK-1, RISK-2, RISK-3, EDGE-2) from 2030-REVIEW.md are documented
with targeted inline comments. No behavioral changes to either hook. Build passes.
</success_criteria>

<output>
After completion, create `.planning/quick/2033-add-targeted-code-comments-for-the-remai/2033-SUMMARY.md`
</output>
