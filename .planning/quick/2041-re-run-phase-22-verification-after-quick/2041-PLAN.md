---
phase: 22-token-burn-rate-budget-alerts
plan: 2041
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-token-burn-rate-budget-alerts/22-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TOKN-10
  - TOKN-11
  - TOKN-13

must_haves:
  truths:
    - "Fresh VERIFICATION.md reflects current code state (post quick-2040 fixes)"
    - "All 8 observable truths from 22-02-PLAN.md are re-evaluated against actual source code"
    - "Phase 22 marked complete in ROADMAP.md (plan checkboxes) and STATE.md if all truths pass"
  artifacts:
    - path: ".planning/phases/22-token-burn-rate-budget-alerts/22-VERIFICATION.md"
      provides: "Fresh verification report with updated status"
      contains: "status:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 22 plan checkboxes marked complete"
      contains: "[x] 22-01-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Updated project state reflecting Phase 22 completion"
      contains: "Phase 22"
  key_links:
    - from: ".planning/phases/22-token-burn-rate-budget-alerts/22-VERIFICATION.md"
      to: "src/client/components/TokenUsageView.tsx"
      via: "Code inspection of API response key usage"
      pattern: "data\\.(burnRates|statuses)"
---

<objective>
Re-run Phase 22 verification after quick-2040 fixed the two API response key mismatches in TokenUsageView.tsx. Produce a fresh VERIFICATION.md and, if all must-haves pass, mark Phase 22 complete in ROADMAP.md and STATE.md.

Purpose: Confirm the quick-2040 fixes resolved all 4 previously-failing truths (burn rate cards, window selector effect, cost projections, budget progress bars) so Phase 22 can be formally closed and Phase 23 planning can proceed.

Output: Updated 22-VERIFICATION.md, updated ROADMAP.md plan checkboxes, updated STATE.md.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-token-burn-rate-budget-alerts/22-01-PLAN.md
@.planning/phases/22-token-burn-rate-budget-alerts/22-02-PLAN.md
@.planning/phases/22-token-burn-rate-budget-alerts/22-01-SUMMARY.md
@.planning/phases/22-token-burn-rate-budget-alerts/22-02-SUMMARY.md
@.planning/phases/22-token-burn-rate-budget-alerts/22-VERIFICATION.md
@.planning/quick/2040-fix-phase-22-verification-gaps-inline-in/2040-SUMMARY.md
@src/client/components/TokenUsageView.tsx
@src/client/hooks/useBudgetAlerts.ts
@src/client/App.tsx
@src/server/routes/historyRoutes.ts
@src/server/database/DatabaseConnection.ts
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify all Phase 22 must-haves against current source code</name>
  <files>
    .planning/phases/22-token-burn-rate-budget-alerts/22-VERIFICATION.md
  </files>
  <action>
Re-run the full Phase 22 verification by inspecting all source files against the must-haves defined in both 22-01-PLAN.md and 22-02-PLAN.md. This is a code-level re-verification, not a runtime test.

**Step 1: Verify the quick-2040 fixes are in place.**
- Confirm `src/client/components/TokenUsageView.tsx` uses `data.burnRates` (not `data.entries`) in the burn rate fetch callback
- Confirm `src/client/components/TokenUsageView.tsx` uses `data.statuses` (not `data.agents`) in the budget status fetch callback

**Step 2: Re-evaluate all 8 observable truths from 22-02-PLAN.md must_haves.**

For each truth, inspect the relevant source code and determine VERIFIED or FAILED:

1. "Operator sees a burn rate ($/hr) per agent in the token usage view, with an aggregate total row"
   - Check: TokenUsageView renders burnRates state in cards; burnRates populated from `data.burnRates`; aggregate card sums values

2. "Operator can switch between Today, 2-day, and 7-day windows via a shared selector"
   - Check: Window selector buttons exist with correct labels; burnWindow state drives fetch URL; default is 'today'

3. "Operator sees daily and weekly cost projections that recalculate when the window changes"
   - Check: Projection card renders when burnRates.length > 0; uses projectedDailyUsd/projectedWeeklyUsd from burnRates

4. "Operator can set a daily budget per agent via inline input, saving on blur or Enter"
   - Check: Budget input with onBlur/onKeyDown handlers; PUT /api/history/budget-config/:agentId call

5. "Agents with no budget show no progress bar and never trigger alerts"
   - Check: hasBudget guard prevents rendering; getBudgetAlertStatus SQL excludes zero-budget agents

6. "Each budgeted agent shows progress bar: green/amber/red by threshold"
   - Check: budgetStatuses populated from `data.statuses`; getBudgetStatusForAgent returns status; progress bar width/color logic

7. "Small colored dot appears next to History nav when any agent crosses 80%/100%"
   - Check: useBudgetAlerts hook reads data.alertLevel; App.tsx renders badge dot conditionally

8. "History nav badge reflects worst alert level across all agents"
   - Check: historyRoutes.ts computes aggregate alertLevel; useBudgetAlerts returns it

**Step 3: Re-verify all 7 server-side truths from 22-01-PLAN.md must_haves.**

Verify the server layer truths (these passed before but re-confirm for completeness):
- GET /api/history/burn-rate returns correct structure for all 3 windows
- Burn rate response includes projectedDailyUsd and projectedWeeklyUsd
- PUT /api/history/budget-config/:agentId stores/removes budget
- GET /api/history/budget-config/status returns per-agent alert levels
- Budget config with 0 means no budget

**Step 4: Re-verify all required artifacts.**

Check each artifact from both plans exists and contains expected content:
- src/shared/types.ts: BurnRateEntry, BudgetConfig, BudgetAlertStatus, BurnWindow
- src/server/database/DatabaseConnection.ts: budget_config table, getBurnRate, getAllBudgetConfigs, upsertBudgetConfig, getBudgetAlertStatus
- src/server/routes/historyRoutes.ts: four endpoints, /status before /:agentId
- src/client/hooks/useBudgetAlerts.ts: 30s polling, previousRef guard
- src/client/components/TokenUsageView.tsx: window selector, burn rate cards, budget editor, projection card
- src/client/App.tsx: useBudgetAlerts import, badge on desktop + mobile

**Step 5: Re-verify all key links.**

Check each key_link from both plans:
- historyRoutes -> DatabaseConnection (getBurnRate, getBudgetAlertStatus)
- DatabaseConnection -> budget_config table
- useBudgetAlerts -> /api/history/budget-config/status
- TokenUsageView -> /api/history/burn-rate (fetch with correct key parsing)
- TokenUsageView -> /api/history/budget-config (fetch + PUT)
- TokenUsageView -> /api/history/budget-config/status (fetch with correct key parsing)
- App.tsx -> useBudgetAlerts (import, call, badge rendering)

**Step 6: Run automated checks.**

- `npm run typecheck` — must pass with zero errors
- `npm run build` — must produce dist/client/ and dist/server/

**Step 7: Write fresh 22-VERIFICATION.md.**

Replace the existing 22-VERIFICATION.md with a fresh report. Use the same format as the original but update:
- `status:` in frontmatter to `verified` (if all pass) or `gaps_found` (if any fail)
- `score:` to reflect actual pass count
- `verified:` timestamp to current date
- `Re-verification: Yes — after quick-2040 fixes`
- All truth statuses updated to current findings
- All key link statuses updated
- Requirements coverage updated
- Remove or update the gaps section (should be empty if all pass)
- Anti-patterns section should reflect no blockers remain
- Keep human_verification section for visual checks that remain relevant
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run typecheck && npm run build</automated>
    <manual>Read the generated 22-VERIFICATION.md and confirm all 8 truths show VERIFIED status</manual>
  </verify>
  <done>
    - Fresh 22-VERIFICATION.md written with re-verification results
    - All 8 observable truths from 22-02 evaluated against current source
    - All 7 server truths from 22-01 confirmed still passing
    - All artifacts verified present with expected content
    - All key links verified wired correctly
    - TypeScript typecheck and production build both pass
    - Score reflects actual verification results (expected: 8/8)
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark Phase 22 complete in ROADMAP.md and update STATE.md</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
**Only execute this task if Task 1 produced a verification score of 8/8 (all truths verified).**

If any truths failed, skip this task and note the remaining gaps in the summary.

**1. Update ROADMAP.md Phase 22 plan checkboxes:**

Change the two plan list items from unchecked to checked:
```
- [x] 22-01-PLAN.md — Server-side burn rate & budget config...
- [x] 22-02-PLAN.md — Client-side burn rate & alerts...
```

The Phase 22 header line already shows `[x]` and "(completed 2026-03-04)" — keep as-is.

**2. Update STATE.md:**

- Update "Current Position" section:
  - `Status:` to "Phase 22 verified complete — re-verification passed 8/8 truths after quick-2040 fixes"
  - `Last activity:` to "2026-03-04 - Phase 22 re-verification passed (quick-2041)"
  - `Next step:` keep as "Execute Phase 23 — model comparison / cost breakdown by model"

- Add quick task 2041 to the "Quick Tasks Completed" table:
  ```
  | 2041 | Re-run Phase 22 verification — confirm all must-haves pass after quick-2040 fixes | 2026-03-04 | (commit) | [2041-re-run-phase-22-verification-after-quick](./quick/2041-re-run-phase-22-verification-after-quick/) |
  ```

- Update "Session Continuity" section:
  - `Stopped at:` "Completed quick task 2041 (Phase 22 re-verification: 8/8 truths pass)"
  - `Next step:` "Plan and execute Phase 23 — Token Analytics & Export"
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && grep -c "\[x\] 22-0[12]-PLAN.md" .planning/ROADMAP.md</automated>
    <manual>Verify ROADMAP.md shows both 22-01 and 22-02 plan checkboxes as [x]; STATE.md reflects Phase 22 verified complete</manual>
  </verify>
  <done>
    - ROADMAP.md Phase 22 plan list shows both plans checked off ([x])
    - STATE.md updated with verification completion status
    - Quick task 2041 added to the completed quick tasks table
    - Session continuity points to Phase 23 as next step
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes — zero type errors
2. `npm run build` succeeds — dist/client/ and dist/server/ produced
3. 22-VERIFICATION.md exists with `status: verified` and `score: 8/8`
4. All previously-failing truths (1, 2, 3, 6) now show VERIFIED
5. All previously-passing truths (4, 5, 7, 8) still show VERIFIED
6. ROADMAP.md has `[x]` for both 22-01 and 22-02 plan items
7. STATE.md reflects Phase 22 verification complete
</verification>

<success_criteria>
- Phase 22 re-verification produces 8/8 passing truths (up from 4/8)
- Fresh VERIFICATION.md replaces the gaps_found version
- Phase 22 formally closed in project tracking (ROADMAP + STATE)
- Project is ready to proceed to Phase 23 planning
</success_criteria>

<output>
After completion, create `.planning/quick/2041-re-run-phase-22-verification-after-quick/2041-SUMMARY.md`
</output>
