---
phase: quick-2029
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-polish/17-02-PLAN.md
  - .planning/phases/17-polish/17-02-SUMMARY.md
  - .planning/phases/17-polish/17-VERIFICATION.md
  - .planning/v2.2-MILESTONE-AUDIT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Zero unqualified GsdHookLogWatcher.ts references remain in Phase 17 documentation"
    - "All updated references annotate the deletion with context about quick-10"
    - "Historical accuracy preserved — docs note the file existed at Phase 17 time but was deleted in quick-10"
    - "v2.2 milestone audit Phase 17 tech debt item marked as resolved"
  artifacts:
    - path: ".planning/phases/17-polish/17-02-PLAN.md"
      provides: "Updated PLAN with GsdHookLogWatcher references annotated"
    - path: ".planning/phases/17-polish/17-02-SUMMARY.md"
      provides: "Updated SUMMARY with deletion annotation"
    - path: ".planning/phases/17-polish/17-VERIFICATION.md"
      provides: "Updated VERIFICATION with historical note about file deletion"
  key_links: []
---

<objective>
Fix stale GsdHookLogWatcher.ts references across Phase 17 documentation.

Purpose: Quick-10 deleted GsdHookLogWatcher.ts (replaced Hooks tab with Events tab), but Phase 17 docs were written before quick-10 and still reference the file as if it exists. The v2.2 milestone audit flagged this as tech debt. These references create confusion when reading historical documentation — the self-check in 17-02-SUMMARY.md even claims the file exists, which is now false.

Output: Phase 17 docs updated with accurate annotations while preserving historical context about when the file existed and why it was deleted. Milestone audit tech debt item marked resolved.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-polish/17-02-PLAN.md
@.planning/phases/17-polish/17-02-SUMMARY.md
@.planning/phases/17-polish/17-VERIFICATION.md
@.planning/v2.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate GsdHookLogWatcher references in 17-02-PLAN.md and 17-02-SUMMARY.md</name>
  <files>
    .planning/phases/17-polish/17-02-PLAN.md
    .planning/phases/17-polish/17-02-SUMMARY.md
  </files>
  <action>
Update GsdHookLogWatcher references using the annotation pattern from quick-20: first mention uses "GsdHookLogWatcher.ts (later deleted in quick-10)" and subsequent mentions use the short form or drop the reference where the context makes clear the file is gone.

**17-02-PLAN.md changes:**

Frontmatter `files_modified` (line 9):
- Change `src/server/services/GsdHookLogWatcher.ts` to `src/server/services/GsdHookLogWatcher.ts (deleted in quick-10)`

Frontmatter `must_haves.truths` (line 17):
- Change "openSync/closeSync in GsdHookLogWatcher.readNewLines and readLastLines are wrapped in try/finally" to "openSync/closeSync in GsdHookLogWatcher.readNewLines and readLastLines were wrapped in try/finally (GsdHookLogWatcher.ts later deleted in quick-10; spawn handler fd pair in gsdRoutes.ts remains protected)"

Frontmatter `must_haves.artifacts` (lines 24-26):
- Change the `path: "src/server/services/GsdHookLogWatcher.ts"` artifact entry. Update `provides` to "readNewLines and readLastLines with try/finally fd safety (file deleted in quick-10)". Add a note comment after the block: `# NOTE: GsdHookLogWatcher.ts was deleted in quick-10 when Hooks tab was replaced by Events tab`

Objective section (line 42):
- Change "Fix four minor defects: (1) wrap openSync/closeSync with try/finally in the spawn handler and GsdHookLogWatcher for fd safety" to "Fix four minor defects: (1) wrap openSync/closeSync with try/finally in the spawn handler and GsdHookLogWatcher for fd safety (GsdHookLogWatcher.ts later deleted in quick-10)"

Context section (line 60):
- Change `@src/server/services/GsdHookLogWatcher.ts` to `@src/server/services/GsdHookLogWatcher.ts  # deleted in quick-10`

Task 1 `<files>` (line 70):
- Change `src/server/services/GsdHookLogWatcher.ts` to `src/server/services/GsdHookLogWatcher.ts (deleted in quick-10)`

Task 1 action, GsdHookLogWatcher sections (lines 114-134):
- Add a comment at the top of the "GsdHookLogWatcher.ts" subsection: "(Note: GsdHookLogWatcher.ts was deleted in quick-10. This change was applied before deletion and the fd-safety pattern is documented for historical reference.)"

Task 1 `<verify>` (line 139):
- Change "Grep for `openSync` in gsdRoutes.ts and GsdHookLogWatcher.ts" to "Grep for `openSync` in gsdRoutes.ts (GsdHookLogWatcher.ts later deleted in quick-10)"

Task 1 `<done>` (line 143):
- Change "All openSync/closeSync pairs across gsdRoutes.ts (1 pair) and GsdHookLogWatcher.ts (2 pairs) are wrapped in try/finally" to "All openSync/closeSync pairs across gsdRoutes.ts (1 pair) and GsdHookLogWatcher.ts (2 pairs) were wrapped in try/finally (GsdHookLogWatcher.ts later deleted in quick-10)"

Verification section (line 185):
- Change "All 3 openSync/closeSync pairs are wrapped in try/finally — confirmed by grep" to "All 3 openSync/closeSync pairs were wrapped in try/finally — 1 in gsdRoutes.ts (still active), 2 in GsdHookLogWatcher.ts (file deleted in quick-10)"

Success criteria (line 191):
- Change "Every openSync has a matching closeSync in a finally block (3 pairs total: 1 in gsdRoutes.ts, 2 in GsdHookLogWatcher.ts)" to "Every openSync had a matching closeSync in a finally block (3 pairs total: 1 in gsdRoutes.ts still active, 2 in GsdHookLogWatcher.ts — file deleted in quick-10)"

**17-02-SUMMARY.md changes:**

Frontmatter `requires.provides` (line 10):
- Change `"extractContextPressure, useAgentLiveStatus, GsdHookLogWatcher"` to `"extractContextPressure, useAgentLiveStatus, GsdHookLogWatcher (later deleted in quick-10)"`

Frontmatter `key-files.modified` (line 29):
- Change `- src/server/services/GsdHookLogWatcher.ts` to `- src/server/services/GsdHookLogWatcher.ts  # deleted in quick-10`

Accomplishments section (line 61):
- Change "Wrapped all 3 openSync/closeSync pairs in try/finally (1 in gsdRoutes.ts spawn handler, 2 in GsdHookLogWatcher readNewLines/readLastLines)" to "Wrapped all 3 openSync/closeSync pairs in try/finally (1 in gsdRoutes.ts spawn handler, 2 in GsdHookLogWatcher readNewLines/readLastLines — GsdHookLogWatcher.ts later deleted in quick-10)"

Files Created/Modified section (line 74):
- Change `- \`src/server/services/GsdHookLogWatcher.ts\` - try/finally around readNewLines and readLastLines fd operations` to `- \`src/server/services/GsdHookLogWatcher.ts\` - try/finally around readNewLines and readLastLines fd operations (file later deleted in quick-10 when Hooks tab was replaced by Events tab)`

Self-Check section (line 102):
- Change `- [x] src/server/services/GsdHookLogWatcher.ts exists` to `- [x] src/server/services/GsdHookLogWatcher.ts existed at Phase 17 time (later deleted in quick-10)`

Do NOT alter git commit message text (these are historical records — commits `be449cf` and `e28f109` must remain verbatim).
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && grep -n 'GsdHookLogWatcher' .planning/phases/17-polish/17-02-PLAN.md .planning/phases/17-polish/17-02-SUMMARY.md | grep -v 'deleted in quick-10\|later deleted\|existed at Phase' | wc -l | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No unqualified GsdHookLogWatcher references"; else echo "FAIL: {} unqualified references remain"; exit 1; fi'</automated>
    <manual>Read both files and confirm every GsdHookLogWatcher occurrence includes deletion context or is preserved historical commit message text</manual>
  </verify>
  <done>17-02-PLAN.md and 17-02-SUMMARY.md contain no bare GsdHookLogWatcher references — all occurrences annotated with deletion context ("deleted in quick-10", "later deleted", or "existed at Phase 17 time")</done>
</task>

<task type="auto">
  <name>Task 2: Update 17-VERIFICATION.md and v2.2 milestone audit</name>
  <files>
    .planning/phases/17-polish/17-VERIFICATION.md
    .planning/v2.2-MILESTONE-AUDIT.md
  </files>
  <action>
**17-VERIFICATION.md changes:**

Truth row #7 in the Observable Truths table (line 38):
- Change "openSync/closeSync in GsdHookLogWatcher readNewLines and readLastLines are wrapped in try/finally | VERIFIED | GsdHookLogWatcher.ts lines 66–71 (readNewLines) and 94–99 (readLastLines): both use `try { fs.readSync(...) } finally { fs.closeSync(...) }`"
- To: "openSync/closeSync in GsdHookLogWatcher readNewLines and readLastLines were wrapped in try/finally | VERIFIED (historical) | GsdHookLogWatcher.ts lines 66–71 and 94–99 had try/finally at Phase 17 time; file deleted in quick-10 when Hooks tab was replaced by Events tab — spawn handler fd pair in gsdRoutes.ts remains protected"

Plan 17-02 Artifacts table (line 62):
- Change "`src/server/services/GsdHookLogWatcher.ts` | readNewLines and readLastLines with try/finally fd safety | VERIFIED | Both methods wrap fs.readSync in try/finally with fs.closeSync in finally block"
- To: "`src/server/services/GsdHookLogWatcher.ts` | readNewLines and readLastLines with try/finally fd safety | VERIFIED (historical — file deleted in quick-10) | Both methods wrapped fs.readSync in try/finally at Phase 17 time; file deleted in quick-10"

FIX-01 requirements row (line 91):
- Change "FIX-01 | 17-02-PLAN.md | Wrap openSync/closeSync in spawn handler with try/finally for fd safety | SATISFIED | gsdRoutes.ts spawn handler + GsdHookLogWatcher readNewLines and readLastLines — 3 pairs total wrapped in try/finally"
- To: "FIX-01 | 17-02-PLAN.md | Wrap openSync/closeSync in spawn handler with try/finally for fd safety | SATISFIED | gsdRoutes.ts spawn handler (still active) + GsdHookLogWatcher readNewLines and readLastLines (3 pairs at Phase 17 time; GsdHookLogWatcher.ts later deleted in quick-10)"

Summary section FIX-01 text (line 146):
- Change "FIX-01 (fd safety): All 3 openSync/closeSync pairs are wrapped in try/finally — 1 in gsdRoutes.ts spawn handler, 2 in GsdHookLogWatcher.ts (readNewLines, readLastLines)."
- To: "FIX-01 (fd safety): All 3 openSync/closeSync pairs were wrapped in try/finally at Phase 17 time — 1 in gsdRoutes.ts spawn handler (still active), 2 in GsdHookLogWatcher.ts (readNewLines, readLastLines). GsdHookLogWatcher.ts was subsequently deleted in quick-10 when the Hooks tab was replaced by the Events tab."

Do NOT change the PASSED status, the 9/9 score, the verified date, or any other content not related to GsdHookLogWatcher.

**v2.2-MILESTONE-AUDIT.md changes:**

Find the `phase: 17-polish` tech debt entry and update its item:
- Change: `"Phase 17 VERIFICATION.md references GsdHookLogWatcher.ts fd safety — file was later deleted by quick-10; remaining spawn handler fd pair still protected"`
- To: `"RESOLVED (quick-2029): Phase 17 docs annotated with GsdHookLogWatcher.ts deletion context from quick-10"`
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && grep -n 'GsdHookLogWatcher' .planning/phases/17-polish/17-VERIFICATION.md | grep -v 'deleted in quick-10\|later deleted\|deleted by quick-10\|at Phase 17' | wc -l | xargs -I{} sh -c 'if [ {} -eq 0 ]; then echo "PASS: No unqualified GsdHookLogWatcher references"; else echo "FAIL: {} unqualified references remain"; exit 1; fi'</automated>
    <manual>Verify 17-VERIFICATION.md still shows status: passed and score: 9/9 — only GsdHookLogWatcher annotations changed</manual>
  </verify>
  <done>17-VERIFICATION.md has no bare GsdHookLogWatcher references — all annotated with deletion context. v2.2-MILESTONE-AUDIT.md Phase 17 tech debt item marked RESOLVED.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rn 'GsdHookLogWatcher' .planning/phases/17-polish/` returns ONLY lines containing deletion context ("deleted in quick-10", "later deleted", "existed at Phase")
2. `grep 'RESOLVED.*quick-2029' .planning/v2.2-MILESTONE-AUDIT.md` returns the resolved entry
3. `npm run build` passes — this is a documentation-only change but verify no accidental source edits
</verification>

<success_criteria>
- Zero unqualified GsdHookLogWatcher.ts references remain in any Phase 17 documentation file
- Every occurrence includes annotation context: "deleted in quick-10", "later deleted in quick-10", or "existed at Phase 17 time"
- Historical commit messages (be449cf, e28f109) preserved verbatim
- 17-VERIFICATION.md status: passed and score: 9/9 unchanged
- v2.2 milestone audit Phase 17 tech debt item marked RESOLVED (quick-2029)
- Build still passes
</success_criteria>

<output>
After completion, create `.planning/quick/2029-fix-remaining-tech-debt-update-phase-17-/2029-SUMMARY.md`
</output>
