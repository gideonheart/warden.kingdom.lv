---
phase: 22-token-burn-rate-budget-alerts
plan: 2040
type: execute
wave: 1
depends_on: []
files_modified:
  - src/client/components/TokenUsageView.tsx
autonomous: true
requirements: [TOKN-10, TOKN-11, TOKN-13]

must_haves:
  truths:
    - "Burn rate cards render per-agent and aggregate totals from API data"
    - "Window selector (Today/2-day/7-day) triggers visible data changes"
    - "Cost projection card renders when burn rate data exists"
    - "Budget progress bars render with correct green/amber/red thresholds"
  artifacts:
    - path: "src/client/components/TokenUsageView.tsx"
      provides: "Corrected API response key mappings"
      contains: "data.burnRates"
  key_links:
    - from: "src/client/components/TokenUsageView.tsx"
      to: "/api/history/burn-rate"
      via: "fetch in useEffect, response mapped via data.burnRates"
      pattern: "data\\.burnRates"
    - from: "src/client/components/TokenUsageView.tsx"
      to: "/api/history/budget-config/status"
      via: "fetch in fetchBudgetData, response mapped via data.statuses"
      pattern: "data\\.statuses"
---

<objective>
Fix two API response key mismatches in TokenUsageView.tsx that prevent burn rate cards, cost projections, and budget progress bars from rendering.

Purpose: Phase 22 verification found 4/8 truths failing due to two wrong property names in fetch response handling. The server returns `{ burnRates }` and `{ statuses }` but the client reads `data.entries` and `data.agents` respectively. Both are single-word fixes that unblock all burn rate and budget UI features.

Output: Working TokenUsageView that correctly maps API responses, passing typecheck and production build.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-token-burn-rate-budget-alerts/22-VERIFICATION.md
@src/client/components/TokenUsageView.tsx
@src/server/routes/historyRoutes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API response key mismatches in TokenUsageView.tsx</name>
  <files>src/client/components/TokenUsageView.tsx</files>
  <action>
    Two targeted one-word fixes:

    1. Line 106: Change `setBurnRates(data.entries ?? [])` to `setBurnRates(data.burnRates ?? [])`.
       Reason: GET /api/history/burn-rate returns `{ burnRates: BurnRateEntry[], window: string }`. The client was reading `data.entries` which is always undefined, causing burnRates state to stay []. This blocked burn rate cards, aggregate row, window selector effect, and projection card rendering.

    2. Line 133: Change `setBudgetStatuses(data.agents ?? [])` to `setBudgetStatuses(data.statuses ?? [])`.
       Reason: GET /api/history/budget-config/status returns `{ alertLevel: string, statuses: BudgetAlertStatus[] }`. The client was reading `data.agents` which is always undefined, causing budgetStatuses state to stay []. This blocked budget progress bar rendering.

    No other changes needed. The server layer, database, shared types, useBudgetAlerts hook, and App.tsx badge are all wired correctly.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run typecheck && npm run build</automated>
    <manual>After build, start production server and navigate to History view. Burn rate cards should display per-agent $/hr values. Setting a budget should show a progress bar with correct color thresholds.</manual>
  </verify>
  <done>
    - `data.burnRates` used on line 106 (not `data.entries`)
    - `data.statuses` used on line 133 (not `data.agents`)
    - TypeScript typecheck passes with zero errors
    - Production build succeeds (dist/client/ and dist/server/ generated)
  </done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes (no type errors)
- `npm run build` succeeds (production artifacts generated)
- `grep -n "data.entries" src/client/components/TokenUsageView.tsx` returns no matches
- `grep -n "data.agents" src/client/components/TokenUsageView.tsx` returns no matches
- `grep -n "data.burnRates" src/client/components/TokenUsageView.tsx` returns a match at the burn rate fetch
- `grep -n "data.statuses" src/client/components/TokenUsageView.tsx` returns a match at the budget status fetch
</verification>

<success_criteria>
All four previously-failing Phase 22 verification truths are unblocked:
1. Burn rate cards render per-agent $/hr with aggregate total
2. Window selector produces visible data changes
3. Cost projection card renders when burn rate data exists
4. Budget progress bars render with green/amber/red thresholds
</success_criteria>

<output>
After completion, create `.planning/quick/2040-fix-phase-22-verification-gaps-inline-in/2040-SUMMARY.md`
</output>
