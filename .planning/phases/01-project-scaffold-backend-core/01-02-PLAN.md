---
phase: 01-project-scaffold-backend-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/backend-verify.sh
  - package.json
  - README.md
autonomous: false

must_haves:
  truths:
    - "tmux sessions matching agent naming convention are auto-discovered and tracked in SQLite"
    - "Terminal output from tmux sessions streams via Socket.IO with under 100ms latency"
    - "ANSI escape codes and colors from Claude Code output render correctly"
    - "User can stop a tmux session from the dashboard"
  artifacts:
    - path: "tests/backend-verify.sh"
      provides: "Automated backend verification script"
      min_lines: 50
    - path: "package.json"
      provides: "Test script for backend verification"
      contains: "test:backend"
    - path: "README.md"
      provides: "Documentation for running backend tests"
      contains: "Backend Verification"
  key_links:
    - from: "tests/backend-verify.sh"
      to: "curl http://127.0.0.1:3001/api/health"
      via: "health check verification"
      pattern: "curl.*api/health"
    - from: "tests/backend-verify.sh"
      to: "tmux new-session -d -s gideon-test"
      via: "test session creation"
      pattern: "tmux new-session.*gideon"
    - from: "tests/backend-verify.sh"
      to: "/api/instances"
      via: "auto-discovery verification"
      pattern: "curl.*api/instances"
---

<objective>
Create comprehensive backend verification infrastructure to test Phase 1 requirements independently without UI, including automated scripts for tmux session discovery, terminal streaming verification, and graceful shutdown testing.

Purpose: Provides confidence that backend meets all Phase 1 success criteria before moving to frontend integration. Enables independent backend testing for regression prevention.

Output: Executable test suite with clear pass/fail indicators, documented in README for future developers.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-backend-core/01-RESEARCH.md
@.planning/phases/01-project-scaffold-backend-core/01-01-SUMMARY.md

@src/server/index.ts
@src/server/database/DatabaseConnection.ts
@src/server/services/TmuxSessionManager.ts
@src/server/services/TerminalStreamService.ts
@src/server/services/InstanceTracker.ts
@src/server/routes/instanceRoutes.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Create backend verification script</name>
  <files>tests/backend-verify.sh</files>
  <action>
Create comprehensive bash script `tests/backend-verify.sh` that verifies all Phase 1 success criteria without requiring UI.

Script structure:

1. **Setup**: Create tests/ directory, make script executable
2. **Health Check**: Verify server is running on 127.0.0.1:3001
3. **Session Auto-Discovery**: Create test tmux sessions with agent naming convention, verify they appear in `/api/instances`
4. **Session Management**: Test session stop endpoint, verify session is killed
5. **Database Persistence**: Verify SQLite database exists with WAL mode, check schema tables
6. **Graceful Shutdown**: Send SIGTERM to server, verify clean shutdown with no zombie processes
7. **Pass/Fail Summary**: Print colored output (green PASS, red FAIL) with counts

Test cases to include:
- `tmux new-session -d -s gideon-test-project` → verify appears in API
- `curl -X POST http://127.0.0.1:3001/api/instances/:id/stop` → verify session killed
- `ls data/warden.db data/warden.db-wal` → verify WAL mode files exist
- `curl http://127.0.0.1:3001/api/health` → verify 200 response

Use `set -euo pipefail` for strict error handling. Clean up test sessions in trap handler.

Reference: RESEARCH.md success criteria, Phase 1 requirements (SESS-01, SESS-04, SESS-05, SESS-06, INFR-02)
  </action>
  <verify>
Check script exists and is executable:
```bash
ls -la tests/backend-verify.sh
```

Run script in dry-run mode (check syntax):
```bash
bash -n tests/backend-verify.sh
```
  </verify>
  <done>Executable backend-verify.sh script exists with tests for health check, auto-discovery, session management, database persistence, and graceful shutdown</done>
</task>

<task type="auto">
  <name>Add test script to package.json and document in README</name>
  <files>package.json, README.md</files>
  <action>
**In package.json:**

Add test script to scripts section:
```json
"test:backend": "bash tests/backend-verify.sh"
```

This provides standard npm interface for running backend tests.

**In README.md:**

Add new section "Backend Verification" with:
- Purpose: Test Phase 1 backend without UI
- Prerequisites: tmux installed, server running on port 3001
- How to run: `npm run test:backend`
- What it tests: List of verification steps (health check, auto-discovery, session management, database, graceful shutdown)
- Expected output: Pass/fail summary with colored indicators

Include example output showing successful test run.

If README.md doesn't exist, create it with project title, description, and this Backend Verification section.
  </action>
  <verify>
Check package.json has test script:
```bash
grep "test:backend" package.json
```

Check README has Backend Verification section:
```bash
grep -A 5 "Backend Verification" README.md
```
  </verify>
  <done>package.json has test:backend script, README.md documents how to run backend verification and what it tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 1 backend functionality</name>
  <what-built>
Backend verification script and test infrastructure. Server should be running with graceful shutdown, connection state recovery, and PTY cleanup from Plan 01.
  </what-built>
  <how-to-verify>
**Step 1: Start the server**
```bash
npm run dev
```

Wait for log: `[Warden] Server running at http://127.0.0.1:3001`

**Step 2: Run backend verification in new terminal**
```bash
npm run test:backend
```

**Expected results:**
- ✅ Health check: Server responds 200 OK
- ✅ Auto-discovery: Test tmux session appears in `/api/instances`
- ✅ Session stop: Session is killed via API
- ✅ Database: WAL mode enabled, tables exist
- ✅ Graceful shutdown: Clean exit with no zombies

**Step 3: Test graceful shutdown manually**
```bash
# In server terminal, press Ctrl+C
# Check logs show:
# - "Killing N active PTY processes"
# - "Checkpointing WAL before close"
# - "Database closed"
# - "Socket.IO server closed"
```

**Step 4: Verify no zombie processes**
```bash
ps aux | grep defunct
# Should show no results (or only grep itself)
```

**Step 5: Check database integrity**
```bash
ls -la data/warden.db*
# Should show warden.db (main file)
# WAL/SHM files may exist during runtime but clean up after checkpoint
```

Report any failures with error messages from verification script.
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass and shutdown is clean, or describe any failures/issues.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 Core Infrastructure is complete when:

1. **Auto-discovery works**: tmux sessions with agent naming convention appear in SQLite database
2. **Terminal streaming ready**: Backend infrastructure supports Socket.IO terminal connections (verified via health check and active stream count)
3. **Session management works**: Sessions can be stopped via API, tmux sessions are killed
4. **Database persistence works**: SQLite with WAL mode stores session metadata
5. **Graceful shutdown works**: Server exits cleanly with PTY cleanup and WAL checkpoint
6. **Verification script passes**: All automated tests show green PASS status
7. **Documentation exists**: README explains how to run backend tests

Ready to move to Phase 2 (Terminal Integration with UI) after human verification confirms all tests pass.
</verification>

<success_criteria>
- [ ] tests/backend-verify.sh exists and is executable
- [ ] Script tests health check, auto-discovery, session stop, database, and graceful shutdown
- [ ] package.json has "test:backend" script
- [ ] README.md has "Backend Verification" section with instructions
- [ ] Running `npm run test:backend` produces clear pass/fail output
- [ ] Manual graceful shutdown test shows proper cleanup sequence in logs
- [ ] No zombie processes exist after shutdown (`ps aux | grep defunct` is empty)
- [ ] Human verification confirms all Phase 1 success criteria are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-backend-core/01-02-SUMMARY.md` documenting:
- Backend verification test coverage
- Test script structure and approach
- Manual verification steps performed
- Phase 1 success criteria confirmation
- Files created and testing patterns used
- Any issues discovered and resolutions
</output>
