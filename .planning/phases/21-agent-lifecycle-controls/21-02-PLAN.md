---
phase: 21-agent-lifecycle-controls
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/client/components/AgentSidebar.tsx
  - src/client/components/InstanceTabBar.tsx
  - src/client/components/TerminalView.tsx
  - src/client/components/gsdShared.tsx
  - src/client/hooks/useActiveInstances.ts
  - src/client/App.tsx
autonomous: false
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
must_haves:
  truths:
    - "Operator sees a Start button on each agent in the sidebar; clicking it creates a new tab with a pulsing yellow Starting indicator"
    - "Operator sees a confirmation dialog before stopping a session; after confirming, the tab shows a pulsing orange Stopping badge"
    - "Operator can restart a stopped session from the tab bar; restart triggers stop-then-start with same identity"
    - "Session tabs show four distinct lifecycle badges: green=active, yellow-pulsing=starting, orange-pulsing=stopping, gray=stopped, red=error"
    - "Start button is disabled when the agent already has an active session"
    - "Terminal area shows contextual overlay when session is starting or stopped"
    - "Force Kill button appears during stopping state for instant kill"
  artifacts:
    - path: "src/client/components/AgentSidebar.tsx"
      provides: "Start button per agent with disabled state for active agents"
      contains: "handleStartAgent"
    - path: "src/client/components/InstanceTabBar.tsx"
      provides: "Lifecycle badges, confirmation dialogs, restart and force-kill buttons"
      contains: "handleRestart"
    - path: "src/client/components/TerminalView.tsx"
      provides: "Starting/stopping/stopped overlays in terminal area"
      contains: "starting"
    - path: "src/client/components/gsdShared.tsx"
      provides: "Extended STATUS_COLORS with starting and stopping entries"
      contains: "starting"
    - path: "src/client/hooks/useActiveInstances.ts"
      provides: "Includes starting and stopping statuses in instance list"
      contains: "starting"
  key_links:
    - from: "src/client/components/AgentSidebar.tsx"
      to: "/api/instances/start"
      via: "fetch POST on Start button click"
      pattern: "fetch.*api/instances/start"
    - from: "src/client/components/InstanceTabBar.tsx"
      to: "/api/instances/:id/stop"
      via: "fetch POST after confirmation dialog"
      pattern: "fetch.*api/instances.*stop"
    - from: "src/client/components/InstanceTabBar.tsx"
      to: "/api/instances/:id/restart"
      via: "fetch POST on Restart button click"
      pattern: "fetch.*api/instances.*restart"
    - from: "src/client/App.tsx"
      to: "src/client/hooks/useActiveInstances.ts"
      via: "activeInstances filter now includes starting/stopping states"
      pattern: "instance\\.status.*starting|stopping"
---

<objective>
Build the client-side lifecycle controls that let operators start, stop, and restart agent sessions from the dashboard with visual lifecycle state indicators and safety dialogs.

Purpose: Complete the Phase 21 user experience — the operator can see and control agent lifecycles without leaving the Warden UI.

Output: Start button in AgentSidebar, confirmation dialogs in InstanceTabBar, lifecycle badge colors, terminal area overlays for transitional states, and Force Kill escape hatch.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-agent-lifecycle-controls/21-CONTEXT.md
@.planning/phases/21-agent-lifecycle-controls/21-01-SUMMARY.md

@src/client/App.tsx
@src/client/components/AgentSidebar.tsx
@src/client/components/InstanceTabBar.tsx
@src/client/components/TerminalView.tsx
@src/client/components/gsdShared.tsx
@src/client/hooks/useActiveInstances.ts
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle badges, Start button, confirmation dialogs, and terminal overlays</name>
  <files>
    src/client/components/gsdShared.tsx
    src/client/components/AgentSidebar.tsx
    src/client/components/InstanceTabBar.tsx
    src/client/components/TerminalView.tsx
    src/client/hooks/useActiveInstances.ts
    src/client/App.tsx
  </files>
  <action>
**src/client/components/gsdShared.tsx:**
- Extend `STATUS_COLORS` map with entries for the new lifecycle states:
  - `starting: 'bg-warden-warning animate-pulse'` (yellow pulsing)
  - `stopping: 'bg-warden-error/60 animate-pulse'` (orange pulsing — use warden-error at 60% opacity to distinguish from stopped which uses full warden-error)

**src/client/hooks/useActiveInstances.ts:**
- Update the `activeInstances` filter in App.tsx (NOT in the hook itself — the hook returns all instances). In `App.tsx`, the `activeInstances` useMemo currently filters for `status === 'active' || status === 'idle'`. Extend this to also include `'starting'` and `'stopping'`: `instance.status === 'active' || instance.status === 'idle' || instance.status === 'starting' || instance.status === 'stopping'`.
- ALSO add `'stopped'` to the filter so stopped tabs remain visible (per user decision: "Tab stays visible with a stopped badge after shutdown — operator can restart from there or manually dismiss"). This is a significant UX change: stopped tabs now show in the tab bar. Limit to sessions stopped within the last 30 minutes to avoid clutter — check `instance.lastActiveAt` against `Date.now() - 30 * 60 * 1000`.

  Actually, a simpler approach: keep the existing useActiveInstances hook as-is (returns all instances from the API). In App.tsx, change the `activeInstances` useMemo filter to include all non-ancient-stopped sessions. The API already returns all instances via syncWithTmux, but `listActiveInstances()` on the DB side filters. So we need to update `listActiveInstances()` in Plan 01 to include transitional states. For stopped tabs: we can show them in the tab bar if they were stopped recently. Better yet, keep it simple — the API should return instances with all lifecycle-relevant statuses. Plan 01 already updated `listActiveInstances` to include starting/stopping. For stopped tabs to appear, also include `'stopped'` but only recent ones (last 30 min) — OR just manage this client-side with a separate state that tracks "recently stopped session names" and shows them until dismissed.

  **SIMPLEST APPROACH:** In App.tsx, change the activeInstances filter to: `instance.status !== 'error'` — this shows active, idle, starting, stopping, AND stopped sessions in the tab bar. The stopped ones will be displayed with gray badges and a Restart button. If the user wants to dismiss them, they disappear when InstanceTracker recycles old records (or we can add a dismiss action later). Per user decision, stopped tabs should remain for restart access.

**src/client/App.tsx:**
- Update the `activeInstances` useMemo to show all non-error instances (or include error too but with a distinct red indicator, per user decision: "Distinct visual treatment for stopped vs errored/crashed: gray for intentional stop, red for crash/error"). Include ALL statuses: `instances` without filtering — let the tab bar show everything and handle visual treatment per status.
  HOWEVER this would clutter with very old stopped sessions. Better: filter to `instance.status !== 'stopped' || isRecentlyStopped(instance)` where recently means last 10 minutes. OR: just show all instances from the API and let the API handle the filter. Actually the simplest: change the DB query in Plan 01's `listActiveInstances()` to include starting/stopping (already done), and for stopped sessions, only show those stopped in last 30 min. But this is Plan 01's job. For Plan 02, just trust the API response and show all instances.

  **FINAL DECISION for App.tsx:** Change the `activeInstances` useMemo to include starting, stopping, and stopped statuses:
  ```
  instances.filter(i => ['active', 'idle', 'starting', 'stopping', 'stopped', 'error'].includes(i.status))
  ```
  This effectively shows ALL instances returned by the API. The API (Plan 01) controls which instances are returned.

- Add a new prop to `AgentSidebar`: `onStartAgent: (agentId: string) => void` and `activeAgentIds: Set<string>` (derived from activeInstances). Pass these down.
- Add `handleStartAgent` callback in App.tsx that calls `POST /api/instances/start` with `{ agentId }`, then calls `refetch()`. On 409, show an alert (or just silently ignore since button should be disabled).
- Add `handleRestartInstance` callback that calls `POST /api/instances/:id/restart`, then calls `refetch()`.
- Add `handleForceKillInstance` callback that calls `POST /api/instances/:id/force-kill`, then calls `refetch()`.
- Pass `onRestart` and `onForceKill` callbacks to InstanceTabBar.

**src/client/components/AgentSidebar.tsx:**
- Accept new props: `onStartAgent?: (agentId: string) => void` and `activeAgentIds?: Set<string>`.
- For each agent in the agent list, add a Start button:
  - Position: inline next to the agent name, on the right side (like the "default" badge position).
  - Text: "Start" (small, warden-success colored).
  - Disabled state: when `activeAgentIds?.has(agent.id)` is true. Show "Running" in muted text instead.
  - On click: call `onStartAgent(agent.id)`. Add `e.stopPropagation()` so it doesn't trigger agent selection.
  - While starting (local loading state): show a small spinner and "Starting..." text.
- Handle 409 response gracefully: if the API returns 409, set an error state briefly ("Already running") then clear.

**src/client/components/InstanceTabBar.tsx:**
- Accept new props: `onRestart?: (instanceId: number) => void` and `onForceKill?: (instanceId: number) => void`.
- Update the status dot rendering to use `STATUS_COLORS` which now includes starting/stopping with animate-pulse.
- **Confirmation dialog for stop:** Replace the current direct `handleStop` with a two-step flow:
  1. Click Stop button → show a simple confirmation inline or as a tiny modal: "Stop {agentName}?" with [Stop] (red) and [Cancel] buttons.
  2. Per user decision: "Minimal quick-confirm style: Stop agent warden-dashboard? with Stop/Cancel buttons".
  3. Implementation: use a local state `confirmingStopSession: string | null`. When set, replace the stop button area with the confirm/cancel buttons. Cancel clears the state.
- **Restart button for stopped sessions:** When `instance.status === 'stopped'`, show a "Restart" button (blue/amber) instead of the Stop button. Clicking it calls `onRestart(instance.id)`.
- **Force Kill button during stopping:** When `instance.status === 'stopping'`, show a "Force Kill" button (red, urgent) alongside a "Stopping..." label. Clicking it calls `onForceKill(instance.id)`.
- **Error badge:** When `instance.status === 'error'`, show a red dot and "Error" label with a Restart button.
- **Dismiss button for stopped tabs:** Add a small "x" button on stopped/error tabs that removes them from view. Use local state `dismissedSessions: Set<string>`. Filter these out from the rendered list.
- Update the existing stop handler to use the new graceful stop endpoint (it already does POST /stop — the backend behavior changes handle the rest).

**src/client/components/TerminalView.tsx:**
- Add an overlay state based on the instance status (needs a new prop or derive from existing `agentLiveStatus`). Actually, TerminalView doesn't currently know the instance status. It only knows `tmuxSessionName` and `agentLiveStatus` (which is about state/pressure from live status polling, not lifecycle status).
- Add a new optional prop: `instanceStatus?: AgentInstanceStatus` (from shared types).
- Pass this from App.tsx by looking up the selected instance's status.
- When `instanceStatus === 'starting'`:
  - Show a full-area overlay on top of the terminal: centered spinner with "Starting {agentName}..." text. Use `bg-warden-bg/80 backdrop-blur-sm` for a frosted glass effect.
  - The xterm terminal underneath should still try to connect (it will fail silently until the tmux session exists, then succeed on retry).
- When `instanceStatus === 'stopping'`:
  - Show a semi-transparent overlay: "Stopping..." with a muted/grayed appearance. Use `bg-warden-bg/60`.
- When `instanceStatus === 'stopped'`:
  - Show a full overlay: "Session stopped" with the Restart button. Use `bg-warden-bg/90`.
  - Per user decision: "grayed-out overlay when stopping/stopped".
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build 2>&1 | tail -20</automated>
    <manual>Open the dashboard, verify Start button appears in sidebar, lifecycle badges update on tabs, confirmation dialog shows for Stop, terminal overlays appear for starting/stopping/stopped states</manual>
  </verify>
  <done>
  - AgentSidebar has Start button per agent, disabled when agent is already running
  - InstanceTabBar shows lifecycle badges (green/yellow-pulse/orange-pulse/gray/red) and confirmation dialogs
  - InstanceTabBar shows Restart button for stopped sessions and Force Kill for stopping sessions
  - TerminalView shows contextual overlays for starting/stopping/stopped states
  - All statuses (starting, stopping, stopped) visible in tab bar
  - npm run build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify lifecycle controls end-to-end</name>
  <files>src/client/components/AgentSidebar.tsx, src/client/components/InstanceTabBar.tsx, src/client/components/TerminalView.tsx</files>
  <action>
  Human verification of the complete agent lifecycle control system:
  - Start button in AgentSidebar that creates a new tmux session with Claude Code
  - Stop with confirmation dialog and 5-second graceful shutdown
  - Restart for stopped sessions
  - Force Kill escape hatch during grace period
  - Real-time lifecycle badges on tabs (starting/active/stopping/stopped/error)
  - Terminal overlays for transitional states

  Steps to verify:
  1. Open Warden dashboard at http://localhost:3001
  2. In the Agent Sidebar, find an agent and click "Start" — verify a new tab appears with yellow pulsing badge and "Starting..." overlay, then transitions to green "active" within 15s
  3. Click Stop on the running session — verify confirmation dialog appears ("Stop agent?")
  4. Confirm the stop — verify tab shows orange pulsing "Stopping..." badge, then transitions to gray "Stopped" within 5s
  5. Click Restart on the stopped tab — verify it goes through starting then active cycle again
  6. Start the same agent again while it's running — verify Start button is disabled (shows "Running")
  7. Start a session, click Stop, then immediately click "Force Kill" during the stopping phase — verify instant termination
  8. Verify `curl -X POST http://localhost:3001/api/instances/start -H 'Content-Type: application/json' -d '{"agentId":"warden"}'` when warden is already running returns 409
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build 2>&1 | tail -5</automated>
    <manual>Perform the 8 verification steps listed in action</manual>
  </verify>
  <done>All 8 verification steps pass: start creates session, stop shows dialog and graceful shutdown, restart works, force kill works, duplicate start prevented, lifecycle badges animate correctly, terminal overlays appear.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors (both client and server)
2. Start button in AgentSidebar is disabled for agents that already have active sessions
3. Stop shows confirmation dialog before executing
4. All four lifecycle badges render with correct colors and animations
5. Terminal overlays appear for starting, stopping, and stopped states
6. Force Kill button appears during stopping state
7. Stopped tabs remain visible with Restart option
8. 409 duplicate-start handled gracefully on client side
</verification>

<success_criteria>
- Operator can start agents from sidebar, stop/restart from tab bar, with visual feedback at every step
- Lifecycle badges (starting/active/stopping/stopped/error) show distinct colors and animations
- Confirmation dialogs prevent accidental stops
- Force Kill provides escape hatch during grace period
- Terminal area shows contextual overlays for non-active states
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/21-agent-lifecycle-controls/21-02-SUMMARY.md`
</output>
