---
phase: 06-close-v1-audit-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "README.md exists in project root with setup instructions"
    - "README.md documents how to run Playwright E2E tests with prerequisites"
    - "README.md documents how to run backend verification tests with prerequisites"
    - "README.md includes troubleshooting guidance for common test failures"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive project documentation with setup, run, and test instructions"
      contains: "Prerequisites"
  key_links: []
---

<objective>
Enhance README.md with comprehensive test documentation including prerequisites, expected output, and troubleshooting.

Purpose: Satisfy requirement TEST-02 (documentation describes how to run Playwright tests) identified as partial in the v1.0 milestone audit. The existing README has basic test commands but lacks prerequisites (tmux, Playwright browsers, Node.js version), expected output examples, and troubleshooting steps.
Output: Complete README.md with structured Testing section.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-close-v1-audit-gaps/06-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance README.md with comprehensive test documentation</name>
  <files>README.md</files>
  <action>
Rewrite the Testing sections of README.md while preserving all existing non-test content (Quick Start, Scripts, Production Deployment, Architecture, API Endpoints). The goal is to replace the current separate "Playwright E2E Tests", "Backend Verification", and "Verifying PTY Resize Safety" sections with a single unified "Testing" section that has proper structure.

Structure the new Testing section as follows:

1. **Testing** (H2 heading) — brief intro: "Three test layers: backend verification, Playwright E2E, and manual verification."

2. **Prerequisites** (H3) — consolidated list:
   - Node.js 22+ (`node --version`)
   - tmux installed (`tmux -V`), with install commands for Ubuntu/Debian (`sudo apt install tmux`) and macOS (`brew install tmux`)
   - Playwright browsers: `npx playwright install chromium`
   - Note: Some tests require the server running on port 3001

3. **Backend Verification** (H3):
   - What: Tests backend functionality without UI
   - Prerequisites note: Server must be running (`npm run dev`)
   - Run commands: `npm run test:backend`
   - "What it tests" bulleted list (health check, auto-discovery, session stop, database, Socket.IO) with checkmark bullets
   - "Expected output" code block showing sample passing output
   - "Troubleshooting" with 3 items: health check fails, auto-discovery fails, database fails

4. **Playwright E2E Tests** (H3):
   - What: Desktop UI tests verify core dashboard flows
   - Prerequisites note: Chromium via `npx playwright install chromium`
   - Run commands: `npm run test:e2e` (headless) and `npm run test:e2e:ui` (interactive)
   - "What it tests" bulleted list (dashboard load, view navigation, agent sidebar, session history, token usage, log viewer, tab bar) with checkmark bullets
   - Test file locations: `tests/e2e/dashboard.spec.ts`, `tests/e2e/screenshot.spec.ts`, `playwright.config.ts`
   - Expected output: `12 passed (12/12)`
   - "Troubleshooting" with 3 items: browser launch fails, tests timeout, server not found

5. **Manual Verification** (H3):
   - PTY Resize Safety sub-section (H4): Keep existing 6-step verification from current README, add expected behavior note
   - Stop Button sub-section (H4): 6 steps to verify stop button works (start dashboard, create test tmux session, click stop, verify loading state, verify session removed from UI, verify with `tmux list-sessions`)

6. **Test Coverage Summary** (H3): Markdown table with columns: Layer, Type, Files, Purpose. Three rows for Backend/E2E/Manual.

Keep the existing README structure order:
- Quick Start (unchanged)
- Scripts (unchanged)
- Testing (new unified section replacing 3 old sections)
- Production Deployment (unchanged)
- Architecture (unchanged)
- API Endpoints (unchanged)
  </action>
  <verify>
Verify README.md exists and contains: (1) "Prerequisites" heading, (2) "npx playwright install chromium" command, (3) "Troubleshooting" heading, (4) "Backend Verification" section, (5) "Playwright E2E Tests" section, (6) "Manual Verification" section, (7) "Test Coverage Summary" table. Run `wc -l README.md` to confirm it's substantially longer than the previous 143 lines.
  </verify>
  <done>
README.md contains comprehensive test documentation with prerequisites (Node.js 22+, tmux, Playwright browsers), run commands for all test types, expected output examples, troubleshooting guidance, and a test coverage summary table. A new contributor can follow the README to successfully run all tests on first attempt.
  </done>
</task>

</tasks>

<verification>
1. README.md exists at project root
2. Contains "Prerequisites" section listing Node.js 22+, tmux, and Playwright chromium
3. Contains "Backend Verification" section with run command and troubleshooting
4. Contains "Playwright E2E Tests" section with run command, test file locations, and troubleshooting
5. Contains "Manual Verification" section with PTY resize safety and stop button verification steps
6. Contains "Test Coverage Summary" table
7. All existing non-test content preserved (Quick Start, Scripts, Production Deployment, Architecture, API Endpoints)
</verification>

<success_criteria>
- TEST-02 fully satisfied: Documentation describes how to run Playwright tests with prerequisites, expected output, and troubleshooting
- README.md serves as single-source onboarding document for new contributors
- All existing README content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps/06-02-SUMMARY.md`
</output>
