---
phase: 06-close-v1-audit-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/openclawTypes.ts
  - src/server/services/OpenClawConfigReader.ts
  - src/client/components/AgentSidebar.tsx
  - src/client/components/InstanceTabBar.tsx
  - src/client/App.tsx
autonomous: true

must_haves:
  truths:
    - "Agent sidebar displays SOUL.md preview text from the agent's workspace directory"
    - "Agent sidebar displays memory status (exists/size) for the selected agent"
    - "Dashboard tab bar has a visible stop button per session that terminates the tmux session"
    - "Stop button shows loading state while stopping and is disabled for already-stopped sessions"
  artifacts:
    - path: "src/shared/openclawTypes.ts"
      provides: "AgentDetails interface with memoryExists and memorySizeBytes fields"
      contains: "memoryExists"
    - path: "src/server/services/OpenClawConfigReader.ts"
      provides: "readSoulPreview and getMemoryStatus private methods"
      contains: "readSoulPreview"
    - path: "src/client/components/AgentSidebar.tsx"
      provides: "SOUL.md preview and memory status display sections"
      contains: "soulPreview"
    - path: "src/client/components/InstanceTabBar.tsx"
      provides: "Stop button per instance tab"
      contains: "handleStop"
    - path: "src/client/App.tsx"
      provides: "onSessionStopped callback wired to InstanceTabBar"
      contains: "onSessionStopped"
  key_links:
    - from: "src/server/services/OpenClawConfigReader.ts"
      to: "src/shared/openclawTypes.ts"
      via: "getAgents returns AgentDetails with soulPreview, memoryExists, memorySizeBytes"
      pattern: "memoryExists.*memorySizeBytes"
    - from: "src/client/components/AgentSidebar.tsx"
      to: "src/shared/openclawTypes.ts"
      via: "renders soulPreview and memoryExists from AgentDetails"
      pattern: "selectedAgent\\.soulPreview|selectedAgent\\.memoryExists"
    - from: "src/client/components/InstanceTabBar.tsx"
      to: "/api/instances/:id/stop"
      via: "fetch POST on stop button click"
      pattern: "fetch.*instances.*stop"
    - from: "src/client/App.tsx"
      to: "src/client/components/InstanceTabBar.tsx"
      via: "passes onSessionStopped prop triggering refetch"
      pattern: "onSessionStopped"
---

<objective>
Close three v1 audit gaps: SOUL.md preview display, memory status display, and stop session button.

Purpose: Satisfy requirements AGNT-02 (agent details sidebar with SOUL.md preview and memory status) and SESS-06 (stop session from dashboard) identified as partial in the v1.0 milestone audit.
Output: Working SOUL.md preview and memory status in agent sidebar, functional stop button in tab bar.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-close-v1-audit-gaps/06-RESEARCH.md
@src/shared/openclawTypes.ts
@src/server/services/OpenClawConfigReader.ts
@src/client/components/AgentSidebar.tsx
@src/client/components/InstanceTabBar.tsx
@src/client/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SOUL.md preview and memory status to server and shared types</name>
  <files>src/shared/openclawTypes.ts, src/server/services/OpenClawConfigReader.ts</files>
  <action>
1. In `src/shared/openclawTypes.ts`, add two optional fields to the `AgentDetails` interface:
   - `memoryExists?: boolean` — whether MEMORY.md file exists in agent workspace
   - `memorySizeBytes?: number | null` — file size in bytes, null if not exists

   The existing `soulPreview: string | null` field is already present (currently hardcoded to null server-side).

2. In `src/server/services/OpenClawConfigReader.ts`:

   a. Add `import { stat } from 'fs/promises';` alongside the existing `readFile` import.

   b. Add a private `readSoulPreview(workspacePath: string): Promise<string | null>` method:
      - If `workspacePath` is empty/falsy, return null
      - Resolve the path: if absolute, use as-is; if relative, join with `path.join(process.env.HOME ?? '/home/forge', '.openclaw', workspacePath)`
      - Read `SOUL.md` from the resolved workspace using `readFile(soulPath, 'utf-8')`
      - Extract first 200 characters, then find the last newline position; if it's past character 100, truncate there and trim; otherwise trim the full 200-char slice
      - Catch ENOENT silently (return null — not all agents have SOUL.md)
      - Log non-ENOENT errors with `console.error('[OpenClawConfig] ...')` and return null

   c. Add a private `getMemoryStatus(workspacePath: string): Promise<{ exists: boolean; sizeBytes: number | null }>` method:
      - If `workspacePath` is empty/falsy, return `{ exists: false, sizeBytes: null }`
      - Resolve path same way as readSoulPreview
      - Use `stat(memoryPath)` on `MEMORY.md` in the resolved workspace
      - Return `{ exists: stats.isFile(), sizeBytes: stats.size }`
      - Catch ENOENT silently (return `{ exists: false, sizeBytes: null }`)
      - Log non-ENOENT errors and return `{ exists: false, sizeBytes: null }`

   d. Modify `getAgents()` method:
      - Change from synchronous `.map()` to `await Promise.all(config.agents.list.map(async (agent) => { ... }))`
      - Inside the async mapper, call `const soulPreview = await this.readSoulPreview(workspace);`
      - Call `const memoryStatus = await this.getMemoryStatus(workspace);`
      - Return the agent object with `soulPreview` (replacing hardcoded `null`), `memoryExists: memoryStatus.exists`, `memorySizeBytes: memoryStatus.sizeBytes`
  </action>
  <verify>
Run `npx tsc --noEmit` from project root. No type errors should appear. Specifically verify that the `AgentDetails` interface is consistent between shared types and the server return.
  </verify>
  <done>
`OpenClawConfigReader.getAgents()` returns agents with real `soulPreview` text (or null if no SOUL.md), `memoryExists` boolean, and `memorySizeBytes` number. The `GET /api/agents` endpoint returns this data without any route changes needed (it already calls `getAgents()` and returns the result).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SOUL.md preview and memory status to sidebar, stop button to tab bar</name>
  <files>src/client/components/AgentSidebar.tsx, src/client/components/InstanceTabBar.tsx, src/client/App.tsx</files>
  <action>
1. In `src/client/components/AgentSidebar.tsx`:

   a. Add a `formatBytes` helper function (inside the component or as a standalone function above it):
      - Accepts `bytes: number | null | undefined`, returns string
      - If null/undefined, return 'N/A'
      - If 0, return '0 bytes'
      - Calculate unit index using `Math.floor(Math.log(bytes) / Math.log(1024))`
      - Return formatted string like "5.2 KB" using `['bytes', 'KB', 'MB', 'GB']` units array

   b. In the Details section (after the existing Model `<div>`), add a SOUL.md preview section:
      - Label: `<span className="text-warden-text-dim">SOUL.md</span>`
      - If `selectedAgent.soulPreview` is truthy: render preview text in `<p className="text-warden-text-dim/80 mt-0.5 text-xs leading-relaxed">`
      - If falsy: render `<p className="text-warden-text-dim/50 mt-0.5 italic">No SOUL.md found</p>`

   c. Below the SOUL.md section, add a Memory status section:
      - Label: `<span className="text-warden-text-dim">Memory</span>`
      - Show a status dot: green (`bg-warden-success`) if `selectedAgent.memoryExists`, dim (`bg-warden-text-dim/30`) if not
      - Text: if memoryExists, show `formatBytes(selectedAgent.memorySizeBytes)`; otherwise show "No MEMORY.md"
      - Use `<div className="flex items-center gap-2 mt-0.5">` for the dot + text layout

2. In `src/client/components/InstanceTabBar.tsx`:

   a. Add `useState` import from 'react'
   b. Extend `InstanceTabBarProps` with optional `onSessionStopped?: (sessionName: string) => void`
   c. Add `const [stoppingSession, setStoppingSession] = useState<string | null>(null)` inside the component
   d. Add an async `handleStop` function that:
      - Takes an `AgentInstance` parameter
      - Sets `stoppingSession` to the instance's tmuxSessionName
      - Calls `fetch(\`/api/instances/${instance.id}/stop\`, { method: 'POST' })`
      - On success: calls `onSessionStopped?.(instance.tmuxSessionName)`
      - On error: logs to console.error and shows `alert('Failed to stop session')`
      - In finally block: sets `stoppingSession` back to null
   e. Restructure each tab from a single `<button>` to a `<div>` wrapper containing:
      - A `<button>` for tab selection (existing click handler, contains status dot + agent name + session suffix + project path)
      - A stop `<button>` with:
        - `onClick` with `e.stopPropagation()` then `handleStop(instance)`
        - `disabled={isStopping || instance.status === 'stopped'}` where `isStopping = stoppingSession === instance.tmuxSessionName`
        - Classes: `px-1.5 py-0.5 text-xs bg-warden-error/20 text-warden-error rounded hover:bg-warden-error/30 disabled:opacity-30 disabled:cursor-not-allowed transition-colors`
        - Title: "Stop session"
        - Text: "Stopping..." when `isStopping`, otherwise a square stop icon or the text "Stop"
   f. Update the wrapper `<div>` styling: `flex items-center gap-2 px-3 py-1.5 rounded-md text-sm whitespace-nowrap transition-colors` plus the existing selected/unselected conditional classes (but move `px-3 py-1.5` to the wrapper, keep only `flex items-center gap-2 flex-1` on the inner selection button)

3. In `src/client/App.tsx`:

   a. Add a `handleSessionStopped` callback (using `useCallback`):
      - Calls `refetch()` to refresh the instance list after a session is stopped
      - Dependency array: `[refetch]`
   b. Pass `onSessionStopped={handleSessionStopped}` prop to `<InstanceTabBar>` in the JSX
  </action>
  <verify>
Run `npx tsc --noEmit` from project root — no type errors. Then run `npm run build` to verify the Vite build succeeds. If a dev server is running, visually confirm: (1) agent sidebar shows SOUL.md preview text or "No SOUL.md found", (2) sidebar shows memory status dot and size, (3) each tab in InstanceTabBar has a red-tinted Stop button.
  </verify>
  <done>
Agent sidebar displays SOUL.md preview text (or graceful "No SOUL.md found" fallback) and memory status with green/dim dot and size. Each session tab has a visible Stop button that shows "Stopping..." while processing and calls the existing `POST /api/instances/:id/stop` endpoint. The instance list refreshes after a successful stop.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully (both server and client)
3. `GET /api/agents` response includes `soulPreview`, `memoryExists`, and `memorySizeBytes` fields for each agent
4. Agent sidebar renders SOUL.md preview or "No SOUL.md found" for selected agent
5. Agent sidebar renders memory status dot (green if exists) and formatted size
6. Each session tab has a Stop button that:
   - Shows "Stopping..." and disables during the API call
   - Is disabled for sessions with status "stopped"
   - Calls POST /api/instances/:id/stop and triggers instance list refresh
</verification>

<success_criteria>
- AGNT-02 fully satisfied: Agent sidebar shows SOUL.md preview and memory status
- SESS-06 fully satisfied: Dashboard has visible stop button that terminates tmux sessions
- All TypeScript types compile cleanly
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps/06-01-SUMMARY.md`
</output>
