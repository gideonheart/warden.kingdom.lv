---
phase: 06-close-v1-audit-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/services/TerminalStreamService.ts
  - src/client/components/TerminalView.tsx
  - src/client/hooks/useTerminalSocket.ts
  - src/shared/types.ts
  - src/client/components/PromptPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Terminal is always interactive — no take-over/release toggle exists"
    - "No isReadOnly state or mode-changed socket events remain in the codebase"
    - "Terminal buffer is never cleared on mode transitions (because there are no mode transitions)"
    - "Prompt panel clearly indicates it sends to OpenClaw Gateway API, not to the terminal"
    - "Take Over / Release buttons are removed from the terminal toolbar"
    - "READ ONLY / INTERACTIVE badge is removed from the terminal toolbar"
  artifacts:
    - path: "src/server/services/TerminalStreamService.ts"
      provides: "Terminal streaming with input always enabled from connection start"
      contains: "terminal:input"
    - path: "src/client/components/TerminalView.tsx"
      provides: "Terminal view without take-over/release UI or isReadOnly gating"
      contains: "sendInput"
    - path: "src/client/hooks/useTerminalSocket.ts"
      provides: "Terminal socket hook without isReadOnly state or mode callbacks"
      contains: "sendInput"
    - path: "src/client/components/PromptPanel.tsx"
      provides: "Prompt panel with clear OpenClaw Gateway label"
      contains: "via OpenClaw Gateway"
  key_links:
    - from: "src/server/services/TerminalStreamService.ts"
      to: "src/client/hooks/useTerminalSocket.ts"
      via: "terminal:input event always active from connection"
      pattern: "terminal:input"
    - from: "src/client/components/TerminalView.tsx"
      to: "src/client/hooks/useTerminalSocket.ts"
      via: "sendInput called unconditionally from onData"
      pattern: "sendInput"
---

<objective>
Remove take-over/release mode complexity and make terminal always interactive. Clarify prompt panel sends to OpenClaw Gateway.

Purpose: Fix Bug 1 (terminal goes blank on mode toggle — root cause: xterm.js instance recreated when isReadOnly changes in useEffect deps). Fix Bug 2 (user confused where prompt goes). User decision: terminal should always be interactive, no toggle needed.
Output: Always-interactive terminals, clear prompt panel labeling.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server/services/TerminalStreamService.ts
@src/client/components/TerminalView.tsx
@src/client/hooks/useTerminalSocket.ts
@src/shared/types.ts
@src/client/components/PromptPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove take-over/release mode from server</name>
  <files>src/server/services/TerminalStreamService.ts, src/shared/types.ts</files>
  <action>
1. In `src/shared/types.ts`:
   - Remove the `TerminalAttachOptions` interface entirely (the `readOnly` field is no longer needed)

2. In `src/server/services/TerminalStreamService.ts`:
   - Remove `isReadOnly: boolean` from the stream tracking interface/object
   - In `handleConnection`: remove the `socket.on('terminal:take-over', ...)` event handler block
   - In `handleConnection`: remove the `socket.on('terminal:release', ...)` event handler block
   - In `attachSocketToSession`: remove the `options: { readOnly: boolean }` parameter (and its default)
   - In `attachSocketToSession`: where input listener is set up, attach it unconditionally (do NOT gate behind `isReadOnly`)
     - The `socket.on('terminal:input', ...)` listener should be registered immediately when the socket attaches, not deferred to enableInputForSocket
   - Remove the `enableInputForSocket()` method entirely
   - Remove the `disableInputForSocket()` method entirely
   - Remove `isReadOnly` from the stream state object
   - In `handleConnection`, change the `attachSocketToSession` call to remove the `{ readOnly: true }` argument
   - Remove the import of `TerminalAttachOptions` if it was imported
  </action>
  <verify>
Run `npx tsc --noEmit` from project root — no type errors. Grep for `isReadOnly`, `take-over`, `release`, `mode-changed`, `enableInput`, `disableInput` in src/server/ — zero matches.
  </verify>
  <done>
Server always enables terminal input from connection start. No mode toggle socket events. No read-only state tracking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove take-over/release mode from client and clarify prompt panel</name>
  <files>src/client/hooks/useTerminalSocket.ts, src/client/components/TerminalView.tsx, src/client/components/PromptPanel.tsx</files>
  <action>
1. In `src/client/hooks/useTerminalSocket.ts`:
   - Remove `const [isReadOnly, setIsReadOnly] = useState(true)` state
   - Remove `socket.on('terminal:mode-changed', ...)` event listener
   - Remove the `setIsReadOnly(true)` reset in cleanup
   - Remove the `requestTakeOver` useCallback
   - Remove the `releaseTakeOver` useCallback
   - Remove `requestTakeOver`, `releaseTakeOver`, and `isReadOnly` from the return object
   - The hook should now only return: `{ sendInput, sendResize, isConnected, isReconnecting }`

2. In `src/client/components/TerminalView.tsx`:
   - Update the destructured return from `useTerminalSocket` — only get `{ sendInput, sendResize, isConnected, isReconnecting }`
   - Remove the `isReadOnly` check wrapping `sendInput(userInput)` — call `sendInput(userInput)` unconditionally in the terminal's `onData` handler
   - **CRITICAL**: Remove `isReadOnly` from the useEffect dependency array (this was the root cause of Bug 1 — terminal instance was recreated on mode change, clearing the buffer)
   - Remove the Take Over / Release button block entirely (the conditional rendering of the two buttons)
   - Remove the READ ONLY / INTERACTIVE badge `<span>` entirely
   - Keep all other toolbar elements (connection status indicator, etc.)

3. In `src/client/components/PromptPanel.tsx`:
   - Change the label text from `"Send prompt to"` to `"Send prompt via OpenClaw Gateway to"`
   - Update the placeholder text from `"Type a prompt... (Ctrl+Enter to send)"` to `"Type a prompt for the agent via OpenClaw Gateway... (Ctrl+Enter to send)"`
   - Add a small help text below the agent selector: `<span className="text-[10px] text-warden-text-dim/50">Sends to agent via Gateway API — not typed into the terminal</span>`
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Run `npm run build` — Vite build succeeds. Grep for `isReadOnly`, `takeOver`, `take-over`, `releaseTakeOver`, `requestTakeOver`, `mode-changed` in src/client/ — zero matches. Verify PromptPanel contains "via OpenClaw Gateway".
  </verify>
  <done>
Terminal is always interactive with no mode toggle UI. useEffect no longer recreates terminal on mode changes (bug fix). Prompt panel clearly labels that prompts go via OpenClaw Gateway, not typed into the terminal.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` completes successfully
3. Zero grep matches for `isReadOnly`, `take-over`, `takeOver`, `releaseTakeOver`, `requestTakeOver`, `mode-changed`, `enableInput`, `disableInput` across all of src/
4. Terminal view has no Take Over / Release buttons
5. Terminal view has no READ ONLY / INTERACTIVE badge
6. Terminal input works immediately on connection (no mode toggle needed)
7. PromptPanel contains "via OpenClaw Gateway" text
8. PromptPanel has help text distinguishing Gateway prompts from terminal input
</verification>

<success_criteria>
- Bug 1 fixed: Terminal never goes blank because xterm.js instance is never recreated on mode change (no mode changes exist)
- Bug 2 fixed: Prompt panel clearly communicates it sends to OpenClaw Gateway API
- INTV-01 removed: No longer "read-only by default" — always interactive per user decision
- INTV-02/03 removed: No toggle needed — always interactive
- INTV-04 simplified: Direct terminal input always available (no mode required)
- Clean removal: No dead code, no unused imports, no orphaned socket events
</success_criteria>

<output>
After completion, create `.planning/phases/06-close-v1-audit-gaps/06-03-SUMMARY.md`
</output>
</content>
</invoke>