---
phase: 18-fix-token-usage-jsonl-session-reader-and-database-population
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/server/index.ts
  - src/server/routes/historyRoutes.ts
  - src/client/components/TokenUsageView.tsx
autonomous: true
requirements:
  - TOKN-04
  - TOKN-05
  - TOKN-06

must_haves:
  truths:
    - "Token usage data appears in the Token Usage tab of the History view with real numbers"
    - "Cache token columns (creation and read) are displayed alongside input/output tokens"
    - "Cost calculations reflect actual API pricing including cache token tiers"
    - "The scanner runs automatically on server startup and periodically thereafter"
    - "Manual scan trigger endpoint exists for on-demand refresh"
  artifacts:
    - path: "src/server/index.ts"
      provides: "SessionUsageReader lifecycle wiring (start on boot, stop on shutdown)"
      contains: "sessionUsageReader"
    - path: "src/server/routes/historyRoutes.ts"
      provides: "POST /api/history/token-usage/scan endpoint for manual trigger"
      contains: "scanAllProjects"
    - path: "src/client/components/TokenUsageView.tsx"
      provides: "Enhanced token usage display with cache token columns and refresh button"
      contains: "cacheCreationInputTokens"
  key_links:
    - from: "src/server/index.ts"
      to: "src/server/services/SessionUsageReader.ts"
      via: "startPeriodicScan on boot, stopPeriodicScan on shutdown"
      pattern: "sessionUsageReader\\.startPeriodicScan"
    - from: "src/client/components/TokenUsageView.tsx"
      to: "/api/history/token-usage"
      via: "fetch in useCallback"
      pattern: "fetch.*token-usage"
---

<objective>
Wire the SessionUsageReader into the server lifecycle, add a manual scan trigger endpoint, and enhance the TokenUsageView UI to display cache token data and cost breakdowns.

Purpose: Plan 01 created the backend service and schema. This plan completes the integration: the scanner starts automatically on server boot, the API returns enriched data, and the UI displays cache tokens and real cost data.

Output: Fully integrated token usage pipeline from JSONL files to UI display, with automatic periodic scanning and manual refresh capability.
</objective>

<execution_context>
@/home/forge/.claude/get-shit-done/workflows/execute-plan.md
@/home/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-fix-token-usage-jsonl-session-reader-and-database-population/18-01-SUMMARY.md

@src/server/index.ts
@src/server/routes/historyRoutes.ts
@src/client/components/TokenUsageView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SessionUsageReader into server lifecycle and add scan endpoint</name>
  <files>src/server/index.ts, src/server/routes/historyRoutes.ts</files>
  <action>
1. **src/server/index.ts:**
   - Import `sessionUsageReader` from `./services/SessionUsageReader.js`
   - After `instanceTracker.startPeriodicSync()`, add `sessionUsageReader.startPeriodicScan()`
   - In `handleShutdown()`, before `instanceTracker.stopPeriodicSync()`, add `sessionUsageReader.stopPeriodicScan()`

2. **src/server/routes/historyRoutes.ts:**
   - Import `sessionUsageReader` from `../services/SessionUsageReader.js`
   - Add a POST endpoint `POST /api/history/token-usage/scan` that:
     a. Calls `await sessionUsageReader.scanAllProjects()`
     b. Returns `{ status: 'ok', message: 'Scan complete' }` with 200
     c. Wraps in try/catch, returns 500 on failure
   - This allows the UI to trigger a manual refresh
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npx tsc --noEmit</automated>
    <manual>Start the server and verify "[SessionUsageReader]" log lines appear showing scan activity</manual>
  </verify>
  <done>Server starts SessionUsageReader on boot, stops on shutdown, and exposes POST /api/history/token-usage/scan for manual trigger</done>
</task>

<task type="auto">
  <name>Task 2: Enhance TokenUsageView to display cache tokens and add refresh button</name>
  <files>src/client/components/TokenUsageView.tsx</files>
  <action>
1. **Update interfaces** to include cache token fields:
   ```typescript
   interface TokenUsageEntry {
     agentId: string;
     date: string;
     inputTokens: number;
     outputTokens: number;
     cacheCreationInputTokens: number;
     cacheReadInputTokens: number;
     costUsd: number;
   }

   interface TokenUsageSummary {
     agentId: string;
     totalInputTokens: number;
     totalOutputTokens: number;
     totalCacheCreationInputTokens: number;
     totalCacheReadInputTokens: number;
     totalCostUsd: number;
     dayCount: number;
   }
   ```

2. **Add a Refresh/Scan button** next to the agent filter input:
   - Button text: "Scan Now"
   - On click: POST to `/api/history/token-usage/scan`, then re-fetch usage data
   - Show a spinner while scanning
   - Use warden theme classes: `bg-warden-accent/20 text-warden-accent hover:bg-warden-accent/30`

3. **Update the Per-Agent Summary cards** to show:
   - Input tokens (direct + cache write + cache read as breakdown)
   - Output tokens
   - Total cost
   - Format: Show "Input: X" with a tooltip or sub-line "(Cache Write: Y, Cache Read: Z)" if cache tokens > 0

4. **Update the Daily Breakdown table** with additional columns:
   - Keep existing: Date, Agent, Input Tokens, Output Tokens, Cost
   - Add: Cache Write, Cache Read columns (compact, show abbreviated numbers)
   - Use `formatNumber()` for all token columns

5. **Improve agent ID display:**
   - The agent_id from the scanner will be a project directory name like `home-forge-warden-kingdom-lv`
   - Add a helper function to make it human-readable: extract the last path segment and clean up dashes (e.g., `warden-kingdom-lv` or just show the full string if short)

6. **Run `npm run build`** to produce production build after all changes.
  </action>
  <verify>
    <automated>cd /home/forge/warden.kingdom.lv && npm run build</automated>
    <manual>Open the dashboard in a browser, navigate to History > Token Usage tab, verify data displays with cache columns and Scan Now button works</manual>
  </verify>
  <done>TokenUsageView displays cache creation and cache read tokens alongside input/output; Scan Now button triggers a refresh; daily breakdown table shows all token categories; production build succeeds</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. Server starts and logs "[SessionUsageReader] Scan complete" within first minute
3. `curl -X POST http://localhost:3001/api/history/token-usage/scan` returns `{"status":"ok"}`
4. `curl http://localhost:3001/api/history/token-usage` returns non-empty usage array with cacheCreationInputTokens and cacheReadInputTokens fields
5. Token Usage tab in browser shows real data with cache token columns
</verification>

<success_criteria>
- Token usage data flows from JSONL files through the scanner into SQLite and displays in the UI
- Cache tokens (write and read) are visible in both summary cards and daily breakdown
- Scan Now button provides manual refresh capability
- Production build succeeds
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-fix-token-usage-jsonl-session-reader-and-database-population/18-02-SUMMARY.md`
</output>
